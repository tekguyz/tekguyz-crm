.env
.env.*
node_modules/
.next/
.claude/settings.local.json
tsconfig.tsbuildinfo
# Python bytecode from scripts/brand
__pycache__/
*.pyc
.claude/settings.json
# Hand-made backups of the above, e.g. settings.json.before-tsfix — same
# machine-local content, so they are ignored for the same reason.
.claude/settings.json.*

# Local MCP server config — machine-specific, and a place credentials can land
.mcp.json
# Supabase CLI link state (project ref), written by `supabase link`
supabase/.temp/
