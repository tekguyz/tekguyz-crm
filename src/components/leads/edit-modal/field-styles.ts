// Shared field styling for EditLeadModal's fieldset siblings. Extracted so the
// four field groups can't drift visually apart — they were four uses of the
// same two local consts before the split.
export const inputClass =
  "w-full rounded-xs border border-hairline bg-canvas-pure p-1.5 text-sm text-ink-main outline-none placeholder:text-ink-muted";

export const labelClass = "mb-1 block text-xs text-ink-muted";
