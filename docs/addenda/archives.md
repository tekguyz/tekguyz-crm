# TEKGUYZ CRM: Archives — Known Gaps history and CLAUDE.md compressions

The two Known Gaps registers plus CLAUDE.md's compression history. Long-lived reference, not dated narrative.

> Part of the TEKGUYZ CRM addenda log. The index — every section, its date and its file — is [`docs/ADDENDA_LOG.md`](../ADDENDA_LOG.md). Cross-references elsewhere in the repo are written as ``docs/ADDENDA_LOG.md § <Section Title>``; that pointer still resolves, via the index.

---

## CLAUDE.md compression history

**Compression history:** 41,369 → 32,786 bytes on 2026-07-30 (narrative re-accumulated *inside* existing bullets was stripped back to rule-or-status plus one pointer), then → 30,416 on 2026-08-11 (resolved Known Gaps relocated to the `ADDENDA_LOG.md` archive; Section 1's duplicated design-token values dropped in favour of `globals.css`). Nothing was ever deleted outright — it moved. **Keep it this way, and note that byte size, not line count, is the health metric here** — both times the file grew, line count barely moved because the bloat was inside existing bullets. A `✅` Known Gaps item does not live here at all, an open one is 1–2 sentences, a completed § 3 initiative is 2–3 sentences, a discipline bullet is the rule itself. The story belongs in the addendum.

2026-08-14: § 2 roadmap and Known Gaps relocated out; file-size cap softened to a smell; "log it when unsure" reversed. 133 → 100 lines. (The plan predicted ~60; its arithmetic ignored that the replacement text for File Size and Build discipline is longer than what it replaced. Nothing was cut to chase the number.)

Two paragraphs from the pre-2026-08-14 Reference Index are preserved here rather than lost, since the replacement index re-expresses their substance more briefly:

> This file was restructured on 2026-07-26 — it had grown to 741 lines / ~150KB, past the point where reading it in full every session (this file's own standing discipline) was practical. Nothing was deleted; the bulk of it moved to two companion docs.

> What stays in *this* file: Section 1 (design system + operational rules + the multi-tenant security model, which is permanent law), the closed 15-phase roadmap, the post-launch feature-work status list (§ 3), the two standing-discipline sections, and Known Gaps — kept as short, current dispositions with a pointer to the full story in `ADDENDA_LOG.md`. New addenda go to `docs/ADDENDA_LOG.md` by default now, not here — this file only gets edited when something becomes a permanent rule/pattern, a post-launch initiative's status changes, or a Known Gaps disposition changes.

### 2026-09-15 — § 3 cut back to status only

CLAUDE.md had reached 89,409 bytes, and § 3 was about 36 KB of it — one row, Shell/IA, was 17.6 KB by itself. The table called itself "status only", but each prompt pack wrote its whole report into its row, and nothing measured it. Every Status cell was cut to one or two sentences: 89,409 → 61,598 bytes. `scripts/check-section-pointers.mjs` check D now fails `npm run check:docs` on a Status cell over 350 characters; it was run against the uncut file first and reported 14 long rows. Most of the cut text already lived in the dated addenda, but not all of it, so every changed row is kept below exactly as it stood. Nothing was deleted — it moved.

| Initiative | Status | Shipped | Full history — `docs/ADDENDA_LOG.md` § |
|---|---|---|---|
| **Leads MEMBER-Role Enforcement** (1 prompt + UI follow-up) | ✅ Complete, applied and proven live (15/15 on `npm run test:rls`). UI follow-up 2026-08-17: `ArchiveControls`/`OutcomeFields` now hide for a MEMBER — presentation only, the trigger is still the boundary | 2026-08-14, UI 2026-08-17 | Leads MEMBER-role enforcement addendum, § 2026-08-17 — Deterministic org resolution, and hiding lifecycle controls from a MEMBER |
| **Per-lead ownership — `leads.assigned_to`** (Prompt 1 of 2) | ✅ Complete, applied and live-verified (26/26 on `npm run test:rls`, including a service-role cross-tenant rejection). Ownership only — **visibility deliberately unchanged**, no RLS policy reads the column | 2026-08-18 | 2026-08-18 — `leads.assigned_to`: per-lead ownership |
| **Team management — role change + member removal** (Prompt 2 of 2) | ✅ Complete, applied and live-verified (43/43 on `npm run test:rls`). Two `SECURITY DEFINER` RPCs; **no RLS policy was added to `organization_members` and none should be** — `authenticated` has no UPDATE/DELETE grant, so RPC-only is enforced below RLS. Removal releases the leaver's `assigned_to` in the same transaction | 2026-08-18 | 2026-08-18 — Team management: role change and member removal |
| **Webhook HMAC request signing** (1 prompt) | ✅ Complete, deployed to production 2026-08-19 and confirmed by a real tekguyz.com submission. Route is `POST /api/v1/triage/<organization_id>`; auth is an `X-TekGuyz-Signature` header over the **raw** body, `crypto.timingSafeEqual`. Old URL-path-secret scheme **removed, not dual-supported**. No schema change. The one real caller, tekguyz.com's contact form, was updated in its sibling repo `C:/Projects/tekguyz-site` | 2026-08-18, deployed 2026-08-19 | 2026-08-18 — HMAC request signing replaces the URL-path webhook secret |
| **Reporting view — `/reports`** (1 prompt) | ✅ Complete, live-verified against a SELECT-only spot check. Read-only, all-time, whole-tenant: pipeline value by `status`, closed leads by `outcome`, win rate = `WON / (WON + LOST)` with `ABANDONED` excluded and the formula printed on the page. **No schema, no migration, no RLS, no role gate** — aggregation runs in JS because PostgREST has no `GROUP BY` and both alternatives are schema work. Nav entry is first in `SECONDARY_NAV`; the mobile tab bar's four slots were not reopened. **Aggregate CSV export (2026-09-14):** `GET /api/reports/export?period=` — one header (`section,item,count,amount,rate`), three sections in page order (pipeline by status, closed by outcome, win rate), following the active period. Every figure comes from the same `getPipelineReport` call the page makes; quoting reuses `escapeCsvValue` from the row-level export; one `formatPercent` (`src/lib/reports/percent.ts`) rounds for the page, `StageLedger` and the file. This month and This year matched the page exactly in the browser. Still no schema, migration, RLS or grant. 544/67 on `npm test` | 2026-08-19, 2026-09-14 | 2026-08-19 — Reporting view: `/reports`, § 2026-09-14 — `/reports` aggregate CSV export |
| **Task editing + non-destructive dismiss** (1 prompt) | ✅ Complete, applied and live-verified through the real UI (edit writes only title/description/due_at; a dismissed task leaves every active surface, keeps its row and its `completed` state). **No DELETE grant and no DELETE policy were added** — re-confirmed live after the migration. One column, `tasks.dismissed`; no new RLS policy needed, since `tasks`' UPDATE policy has no column-level gate | 2026-08-19 | 2026-08-19 — Task editing and non-destructive dismiss |
| **Prospects — cold-outreach staging + promotion** (2 prompts) | ✅ Complete. Prompt 1 (2026-08-26): `prospects` table keyed on `place_id`, `import_prospects_chunk`, `/prospects/import`. Prompt 2 (2026-08-26): `notes` + `promoted_lead_id` columns, the `/prospects` call list, inline status/notes/archive editing, and Promote. **`leads` DDL, RLS, `LEAD_COLUMNS` and the webhook were never touched** and were re-verified unchanged after both migrations. 179/27 on `npm test`, 76/76 on `npm run test:rls` (64 pre-existing unchanged, 12 new). The two leadgen CSVs stay loaded: **122 prospects in TEKGUYZ**, `TEKGUYZ Demo` carries 7 synthetic fixtures from `seed:demo`. **Atomic promotion (2026-09-14):** `promoteProspect` now makes one call to the `SECURITY DEFINER` RPC `promote_prospect`, which locks the prospect `FOR UPDATE`, re-checks the caller's membership for the passed org, then does the `leads` insert, the `lead_submissions` insert and the guarded `where promoted_lead_id is null` claim in one transaction — the lost-race orphan lead can no longer happen, and its logging path is deleted. No RLS policy, table, column or `LEAD_COLUMNS` change. Dry-run 16/16 on temp replicas; applied by the human; 109/109 on `npm run test:rls` (7 new, incl. two concurrent promotions → one lead, one submission, zero orphans, repeated 3×); 516/66 on `npm test`; no new advisor finding | 2026-08-26, 2026-09-14 | 2026-08-26 — `prospects`: cold-outreach staging, its RLS and its CSV import, § 2026-08-26 — Prospect promotion: the write path into `leads`, § 2026-09-14 — Prospect promotion becomes one transaction |
| **Public read-only demo entry point** (1 prompt, P1's first piece) | ✅ Complete, applied and live-verified (26/26 new on `npm run test:rls`, 101/101 total; 234/234 on `npm test`). Read-only is a dedicated **Postgres role, `demo_readonly`, holding `SELECT` only** — never a `VIEWER` value in `organization_members.role`; grants sit below RLS, so writes and all 9 `SECURITY DEFINER` RPCs are denied with **zero policy edits** and a future table is unwritable by default. **No policy in this schema carries a `TO` clause**, which is why the existing read policies cover the new role unchanged. Two migrations; one column, `organizations.is_demo`. Also fixed the standing weekly-report cron leak. Webhook/triage untouched. UI follow-up same day: a neutral `Read-only` badge in the shell header for `is_demo` tenants only (`src/components/shell/DemoModeBadge.tsx`) — presentation only, no grant or policy touched; 238/238 on `npm test` after it. Refusal follow-up 2026-09-14: a thrown `42501` in the demo tenant is tagged with a fixed digest by `demoAwareError` and renders "This demo is read-only" instead of the generic error boundary; any other error, and any `42501` in a real tenant, still shows the normal boundary. Presentation only, no grant or policy touched; 516/66 on `npm test`. Inline follow-up 2026-09-14: actions that return `{ error }` into their form now build it through `demoAwareMessage`, which shares `demoAwareError`'s one `42501` + `isDemo` test, so a demo visitor reads "This demo is read-only…" under the form instead of the raw Postgres text — nine files, lead/team sentinel translations still win first. Verified through the real `/demo` on New Lead and prospect notes; the demo owner's duplicate-email error is unchanged. Presentation only, no grant or policy touched; 538/66 on `npm test` | 2026-09-04, 2026-09-14 | 2026-09-04 — The public read-only demo identity, § 2026-09-04 — The demo read-only badge, and why it is neutral (later same day), § 2026-09-14 — The demo explains a refused write, § 2026-09-14 — The demo explains a refused write inside a form |
| **Invite-only signup + demo entry on `/login`** (1 prompt) | ✅ Complete, live-verified by POSTing the real `signUp` Server Action directly with the page bypassed (no token, wrong email and valid-invite cases all behaving). Public self-serve signup was live on `/signup` and is closed; `/login` now links to `/demo` instead. **No schema, no migration, no RLS, no grant** — routing and presentation only. The invite flow still needs `/signup`, because `/invite/[token]` never created the auth user itself. 250/40 on `npm test` | 2026-09-05 | 2026-09-05 — Signup is invite-only, and `/login` offers the demo |
| **Three registered gaps in one wave — leads CSV export, `/reports` period filter, dead CSS** (1 prompt) | ✅ Complete, live-verified through the real route and the real page (`GET /api/leads/export` returned 200 / `text/csv` / 20 rows with the header exactly `LEAD_COLUMNS`; `/reports` narrowed from 16 open / $146,750 all-time to 5 open / $33,250 for This month). **No schema, no migration, no RLS, no grant** — export is read-only over `LEAD_COLUMNS`, the period filter is two extra `.gte()`/`.lt()` pairs on the two SELECTs that already ran, and the CSS change is one deleted declaration that never painted a pixel. The win-rate formula is untouched; the period only narrows its inputs. Exporting the `/reports` aggregate stayed open then and closed 2026-09-14 (see the Reporting view row). 278/43 on `npm test` | 2026-09-05 | 2026-09-05 — Three registered gaps closed: leads CSV export, the `/reports` period filter, one dead declaration |
| **Global search — command palette** (2 prompts) | ✅ Complete — all three record types covered. Prompt 1 (2026-08-28): tasks alongside contacts. Prompt 2 (2026-09-05): **prospects as the third source** — `searchProspectsForOrg` + `fetchSearchableProspects`, a third labelled group, and `/prospects?highlight=<id>` as the arrival. **No schema, no migration, no policy, no grant** in either prompt — `leads`, `tasks` and `prospects` were already tenant-scoped by the same existing RLS. A task result opens its parent lead's profile sheet; a prospect result scrolls to and marks its table row, because `/prospects` has no per-record detail view. The arrival highlight was **extracted, not copied** — `src/lib/hooks/use-arrival-highlight.ts` is the one implementation and `TasksSection` now consumes it too. Pagination/debounce stays deliberately out and open in `docs/KNOWN_GAPS.md`. 298/46 on `npm test` | 2026-08-28, 2026-09-05 | 2026-08-28 — Command palette: tasks alongside contacts, § 2026-09-05 — Command palette: prospects as the third source |
| **Webhook ingestion failure visibility** (1 prompt) | ✅ Complete. Every failure path on `POST /api/v1/triage/<organization_id>` now prints one structured `[webhook-triage-failure]` JSON line (stable code, `organization_id`, stage, detail) and a signature-valid request that fails to persist also emails the OWNER/ADMIN recipients through the **existing** Resend integration. A **401 never alerts** — it is scanner noise, logged at `warn` only. **No schema, no migration, no RLS, no grant, and no response status code changed for any existing case** — the 500 was already there, uncaught; what is new is that a human hears about it. `signature.ts` untouched; per-tenant rate limiting already existed and was not changed (a 429 logs at `warn` and never alerts), and replay protection stays deliberately held for one later prompt. 289/45 on `npm test` | 2026-09-05 | 2026-09-05 — Webhook ingestion failure visibility |
| **Webhook replay protection + Redis rate limiter** (1 prompt, ROADMAP P2 + P3 folded) | ✅ Complete, deployed 2026-09-14 and confirmed by a real tekguyz.com submission (CRM `f91b849`, site `3f8dda0`, shipped back to back). Hard cutover: the signature now covers `${timestamp}.${rawBody}` with `X-TekGuyz-Timestamp`, ±300s tolerance, and a Redis replay guard (`${organization_id}:${signature}` lowercased, `SET NX EX 600`); the per-tenant limiter moved from counting `activity_logs` to a Redis sorted set, same 30/min sliding window, still a distinct 429. Both guards fail open and log. **The CRM and `C:/Projects/tekguyz-site` must change this protocol together** — any future edit to it is again a back-to-back deploy. Upstash credentials are in `.env` and Vercel, sharing tekguyz.com's database (its keys are `tg:`-prefixed, so none collide). **No schema, no migration, no RLS, no grant, no UI**; new dependency `@upstash/redis`. 495/63 on `npm test`; replay, skew and both fail-open tests proven red first | 2026-09-14 | 2026-09-14 — Webhook replay protection and a Redis-backed rate limiter |
| **Avatar primitive** (1 prompt) | ✅ Primitive built and in production — the lead read panel's header avatar since 2026-09-12, and the shell header's account-menu trigger since 2026-09-13 (Shell/IA Stage 2, prompt 2). `src/components/ui/Avatar.tsx`: initials plus a deterministic FNV-1a hash over the trimmed, lowercased name, indexed into the six decorative pill hues already in `globals.css` — each hue ships a matched `bg`/`fg` pair, so no contrast is computed and no third colour set exists. **No hash-to-colour function existed anywhere in the codebase and one was built fresh here** — `StageLedger` maps a fixed status to a pill, which is not hashing, and the header's `IdentityMenu` used an ad-hoc `name.slice(0, 1)` on a neutral ground. Not built on Radix: `@shadcn/avatar` exists and was queried live, but its Root only coordinates an `<img>` load state and there is no image. **Nothing in `IdentityMenu`, `WorkspaceBlock` or any production route changed**; no schema, no migration, no RLS, no storage bucket. Registered on `/design`, which is where a **whitespace bug the unit tests missed** was caught: `getInitials` collapsed inner whitespace but the hash key only trimmed, so `"Ada Lovelace"` and `"  ada   lovelace  "` rendered as the same initials on two different hues. Fixed and pinned by two regression tests. **This is the one Avatar; a second implementation must not be written** — one was, in parallel on another machine, and was deleted rather than merged (see the Shell/IA row below) | 2026-09-08 | Not yet — no dated addendum; the component's own comments carry the reasoning |
| **Shell/IA redesign pass** (2 stages) | ✅ Complete — **all of Stage 2 is wired: the lead surface, the shell, the pipeline card, and Settings (Variant Split, wired 2026-09-13). The Shell/IA redesign pass is closed.** **STAGE 2, prompt 4 (2026-09-13): Settings — Variant Split — wired to production.** `/settings` is now ONE `Card` surface (`SettingsSplit`) holding four `SettingsSection` rows, each with its name and explanation in a fixed 16rem left column and its controls on the right, a full-bleed hairline between rows, stacking below `md` (`src/components/settings/SettingsSection.tsx`, the copy in the plain module `sections.ts`). The four real panels are `OrgDetailsPanel`, `TeamPanel` (with `InviteMemberForm` and `MemberRow`), `ApiKeysPanel` and `AccountPanel`. **Split could not hold them unchanged:** each panel drew its own `Card` and `<h2>`, which inside a Split row would say the section's name twice, so the owner chose to remove exactly those two wrappers (and API Keys' caption and `HelpTooltip`, which moved to the left column) — every field, `name=`, action, dialog and `useFormResetRestore` call is unchanged. `/settings` never had an `<h1>`, so there was none to drop. Measured in headless Chrome as the demo OWNER: at 1440×800 every row's left column is 256px and its controls 686px, no horizontal overflow anywhere; at 390×844 all four rows stack at 324px, document `scrollWidth` 390 = `clientWidth`. **Parity diff before and after, both directions: five forms, all ten difference lists empty.** `settings-layout.test.ts` pins the four panels once each, no heading or `Card` inside a panel, and every field name in exactly one file. `check:widths` now covers `/settings` and is clean (58 text boxes at both viewports). **No schema, no migration, no RLS, no grant; all five action files byte-for-byte unchanged.** 450/61 on `npm test`, twice. That needed a fix this prompt did not cause: the two lead-drawer form-reset tests had been timing out under full-suite load (the untouched `HEAD` measured 431/432). They typed one character at a time — one controlled re-render per keystroke — and now paste each value once, ~3.7s to ~2.3s alone; the reset assertions are unchanged and the Edit one was re-proven red with the restore hook disabled. The Aug 29 answer to the same symptom had been raising `testTimeout`, which never removed the cause. **STAGE 2, prompt 3 (2026-09-13): the pipeline card — Variant Grouped — wired to production.** `KanbanCard` and `FocusListCard` now both render ONE field block, `src/components/pipeline/PipelineCardFields.tsx`, the Grouped comp's two rows; `card-fields.test.ts` fails if either card grows a copy back (proven red, then green). `FocusListCard` alone gains a third row holding its existing status `<select>`, handler unchanged and still calling `updateLeadStatus`. This IS a visual change to `KanbanCard` — production still carried the pre-Grouped layout, so wiring the pick required it. Measured in headless Chrome: every Kanban card 65px at 1440×800, same as the comp; a phone card 101px with the select; a select change persisted through a reload and was restored. `check:widths` now covers the real `/pipeline` and is clean. **No schema, no migration, no RLS, no grant; `src/lib/actions/`, `KanbanColumn`, `groupLeadsByStatus` and `Card` unchanged.** 432/60 on `npm test`. **STAGE 2, prompt 2 (2026-09-13): the shell — Variant C, Quiet — wired to production, and the read panel's way into the edit drawer.** The shipped sidebar, header and mobile tab bar ARE the Quiet comp now: the sidebar groups destinations by POSITION (Work · Prospecting, a hairline between, a pinned Reports/Settings footer; no captions, no disclosure) via `NAV_GROUPS`/`NAV_FOOTER` in `src/components/shell/nav-items.ts`; the collapse control is a chevron at the panel's top edge; New Lead leads the rail as the shell's one `primary` button; the header is 48px with the page title (`pageTitleFor`, rendered as a `<p>`, never a heading) then Help · Search · avatar menu; the avatar menu is account-scoped only (Help left it, Settings never entered it); the phone tab bar is an inset floating card at Level-1 elevation. `DESIGN.md` § The Application Shell decisions 2 and 3 are rewritten as shipped, and 4 and 5 amended. **Unchanged by design:** the `translate` collapse on an overlaid rail, the `tg_sidebar` cookie read in the server layout (measured: collapse, reload, and the server HTML already carries the 56px spacer), `<main>` `relative`, the four tab-bar slots, and `WorkspaceBlock` as a static placeholder (it gained a layout-only `className` prop and nothing else). **The edit entry point:** `ProfileSheet`'s header carries `Edit lead`, and `EditLeadDrawer` became the ONE host of both lead views — it owns a `view` state, so `View full profile` and `Edit lead` only switch views and every close path closes the whole slot; the `?leadId=` deep link and the command palette now open it with `initialView="read"` instead of mounting a bare `ProfileSheet`. Verified in real headless Chrome from a Contacts card and from a deep link, pre-filled with that lead's real data. **Parity re-run with every group closed: 18 rendered `name=`s = `updateLead`'s 18 `formData.get()`s, both difference lists empty.** **No schema, no migration, no RLS, no grant; `src/lib/leads/actions.ts` and `src/lib/actions/` byte-for-byte unchanged.** `check:widths` clean — its one failure on the way (the bare word `Edit` at 21.8px) was answered by changing the label, not the floor. 427/59 on `npm test`. **STAGE 2, prompt 1 (2026-09-12): the lead surface — the picked read panel and the picked form container, wired together at one width.** They could not be wired apart: they occupy the same slot on screen, which is the basis the Drawer was picked on. `ProfileSheet` is now Variant A (the jump strip) over real lead data, with real section counts reported up by the three list sections; `EditLeadDrawer` and `CreateLeadDrawer` replace `EditLeadModal` and `CreateLeadModal`; `components/leads/edit-modal/` is now `edit-form/` and its five field-group files kept their field sets exactly. All three overlays are the SAME primitive — `src/components/ui/sheet.tsx` gained the `side` prop its own comment had reserved for a real right-edge caller — and the SAME width, `LEAD_PANEL_WIDTH` in `src/components/leads/panel-width.ts`, measured at **672px each at 1440×800** in a real browser and pinned by a source-level test that also forbids either file naming a `max-w-` of its own. The read panel's old `createPortal` is gone with the native `<dialog>` that caused it, and it gained a focus trap it never had. **Field parity was diffed against the wired forms with every group CLOSED, both directions: 18 = 18 on edit, 8 = 8 on create, all four difference lists empty.** The create drawer stays at eight because `createLead` reads eight. **No schema, no migration, no RLS, no grant; `src/lib/leads/actions.ts` byte-for-byte unchanged.** `npm run check:widths` now covers the three REAL surfaces (it clicks them open) as well as the comps, and gained an opt-in `--shots` flag — which is how this pass produced real screenshots with the Browser pane hidden all session. 416/58 on `npm test`. Still Stage-2 work after prompt 3: Settings, which is not even picked. **Stage 1 (design exploration), all four prompts built.** **Prompt 3 (2026-09-11): the pipeline card** — three dev-only restyles of the Kanban card at `/shell/pipeline` (`/line`, `/split`, `/grouped`) over the purpose-built fixture `preview/mock-pipeline.ts` (the demo org's 16 leads all read overdue and would hide the not-cold state). **Variant Grouped picked 2026-09-11** — a header row (name + star, revenue right) over one meta line (company · date · assignee); Line and Split are kept as the record, not maintained. A **restyle, not a density fix**: the field set was fixed — `client_name`, `company`, `is_starred`, `estimated_revenue`, `next_action_at` with `Card`'s existing `cold` prop, `assigned_to` only when set — with no status badge, no `lead_source` and no activity query. One `+N more` control after 10 cards (the shipped `groupLeadsByStatus` order, imported) is built once in `preview/CompBoard.tsx`, so the pick compared layout only. Measured at 1440×800 inside a copy of the shipped shell's geometry: Line 115px / 5 cards visible, Split 81px / 7, Grouped 65px fixed / 8. Grouped's cost is a truncated company on assigned leads; its meta line needed three measured drafts before neither company nor assignee could reach zero width, and no test catches that class. **`KanbanCard`, `FocusListCard`, `KanbanColumn` and `Card` are byte-for-byte unchanged**; no Server Action, query, schema, migration or RLS. Stage 2 notes: the two shipped cards carry identical copies of the field block, `KanbanCard` renders no Avatar today, and `FocusListCard`'s status `<select>` needs a place in Grouped's two rows (field block and select closed 2026-09-13 by Stage 2 prompt 3). Also fixed: `/api/dev-login` now redirects to the request's own origin and takes a same-origin `?next=`, so a dev server on 3001 no longer throws you onto 3000. 352/50 on `npm test`. **Prompt 2 (2026-09-09): the lead/contact detail panel** — four dev-only variants at `/shell/detail` (`/jump`, `/tabs`, `/accordion`, `/split`), all over the static fixture in `preview/mock-lead.ts`. **Variant A — Jump strip picked 2026-09-09**, closed out 2026-09-11 with section counts added to its strip (so it stops reading as a tab bar); B, C and D are kept as the record, not maintained. **A comps index SHOWS the variants, it does not describe them** — the first draft of `/shell/detail` was four cards of prose with no picture of anything, and was rejected on sight as unusable, because choosing between four layouts is a looking task. It now renders each real panel scaled down (`preview/VariantThumb.tsx`); prompts 3 and 4 should start that way. Two defects came out of that rebuild: wrapping a preview in a `<Link>` nests the panel's own `tel:`/`mailto:` anchors inside it and **fails hydration on the whole page**, so the link is an absolutely-positioned sibling overlay; and a preview needs `inert`, not `aria-hidden`, or four panels put ~70 dead controls in the tab order. Section bodies also carry **full-bleed hairline rules** between them, per `plancrm-drawer.webp` — the first draft used a `space-y-6` gap instead, which is what made the panel read as one unbroken column of text. All four share one compact header (avatar + name + the four click-to-action shortcuts on one row, via `Button asChild` rather than a second copy of Contacts' hand-rolled class string) and one 4-up metadata strip whose every field is a real `leads` column — `status`, `estimated_revenue`, `next_action_at`, `lead_source` — verified against the DDL, not a doc. They differ only in section navigation, which is the axis under test: a sticky jump rail with scroll-spy, a tab switcher, a counted accordion, and a wide two-column split. **`src/components/leads/profile/` is byte-for-byte unchanged** (`git diff --stat` empty), no Server Action, no query, no schema, no migration, no RLS. Tasks, the enquiry timeline and the note composer are carried over restyled, not rebuilt. Interactive verification could NOT be done in the browser — the Browser pane was hidden all session, so rAF never fired, React never hydrated (`hydrated: false` measured on a live button) and every click no-opped — so 18 jsdom tests carry that evidence instead; two of them failed first and found real defects, one an accordion header whose accessible name computed as `"Tasks3"`. 334/49 on `npm test`; all five routes prerender a clean 404 with none of their own markup. Prompt 1 of 4: three dev-only shell variants at `/shell/*`; **Variant C — Quiet picked 2026-09-07**, with Help as a header icon and Settings staying a sidebar destination. **Nothing under `src/components/shell/` changed** and no production route was touched. The three variant headers consume the existing `src/components/ui/Avatar.tsx` — this prompt originally built its own `Avatar` plus a `src/lib/ui/avatar-identity.ts`, in parallel with the Avatar-primitive prompt above and unaware of it; **the duplicate was deleted, not merged**, and the headers were rewired to the shipped primitive's `name` prop. The pick re-opens `DESIGN.md` § The Application Shell decisions 2 and 3 — neither that file nor the shipped shell was edited until Stage 2 prompt 2 rewrote both on 2026-09-13. **Prompt 4 (2026-09-11): the lead/contact form and Settings → Org Profile & Branding** — and it is the one Stage 1 prompt with **no pick yet**. Two comp sets over static fixtures: `/shell/form` as three CONTAINERS for ONE form (`/modal`, `/drawer`, `/inline`), and `/shell/settings` as three LAYOUTS for the same three sections (`/stacked`, `/rail`, `/split`). The axis is the container and the layout only: the form's fields, order, labels, controlled-state wiring and submit row live in one shared `preview/FormBody.tsx`, and the Settings sections in one shared `preview/SettingsBlocks.tsx` plus a plain `preview/sections.ts`, so a visible difference is never a typo in one variant. **Edit is Create with values in it** — one component, one layout, `initial` the whole difference. Six fields, prompt 3's trimmed set reused rather than re-derived, and **deliberately not the real form's field set**: Stage 2 owes its own Form/Action field-parity diff against the real `createLead`/`updateLead`. Measured at 1440×800 — form containers 512 / 447 / 672px, all three fitting six fields and the submit row; Settings first control 127 / 127 / 43px down, with Stacked the only variant whose content overflows the 668px box. **No logo upload exists and none was drawn**: `public.organizations` has no logo column, no bucket and no upload action, so "Branding" is the shipped mark via `BrandMark` at two sizes plus the absence stated in words — the same refusal PanelChrome made about a photo on the avatar. Team, API keys and Account are **absent, not stubbed**, and three tests per variant assert that. Also added: **`npm run check:widths`** (`scripts/check-comp-text-widths.mjs`), a real-browser layout check that fails when any visible element owning three or more characters of its own text renders under 24px wide, at 1440×800 and 1024×800, over `?long=1` fixtures — it drives Chrome over the DevTools Protocol with no new dependency, signs in through `/api/dev-login` first so it cannot pass by measuring `/login`, and skips `<option>`s and `.sr-only` text for reasons its own comments carry. **jsdom can never do this job** — it has no layout engine, so every `getBoundingClientRect()` is 0x0 and a width assertion there passes vacuously; that is why the check is a script and not a test. Proven red before green. `VariantThumb` gained two optional size props for the landscape tiles; both existing callers pass neither and are unchanged. `src/lib/actions/`, `supabase/`, the shipped form components and all four Settings panels are byte-for-byte unchanged; no Server Action, query, schema, migration, RLS or grant. 392/55 on `npm test` (36 new, 4 proven to fail) — and the previously recorded 352/50 baseline was itself stale by the 4 tests of `src/app/demo/route.test.ts`. **Review 2026-09-12 — the form container is PICKED: Variant Drawer, "everything in the drawer".** Reading and editing one lead share one address on screen, the slot the picked detail panel already owns; a wide full-page read view was considered and declined because it would reopen the 2026-09-09 detail-panel pick and split one lead across two places. The **Settings layout stays unpicked** (leaning Split, which already covers small screens by stacking below `md`). Two measured findings came with it: the Drawer comp was widened after it was measured at 447px — the same width as the shipped `Modal` the review called too narrow — and a new route `/shell/form/full` puts the **real eighteen-field** edit form in the picked drawer, grouped into four sections, two-up, with the two rarely-touched groups closed and a **pinned action bar**. Measured at 1440×800 across 448/512/672/768px: width changes the height by **zero pixels**, because the two-column grid runs off the `sm:` VIEWPORT breakpoint and not the panel; the grid and the collapse buy the height (425px of overflow → 92px) and the pinned bar is what removes the scroll-to-save. **A collapsed group keeps its inputs mounted and merely `hidden`** — unmounting them would make `updateLead` read null for every column behind a closed group and NULL it on save, so Stage 2's field-parity diff must be run with every group CLOSED. 398/56 on `npm test` | 2026-09-12 (lead surface), 2026-09-13 (shell, pipeline card, Settings) | 2026-09-07 — Shell/IA Stage 1, prompt 1 of 4: the sidebar, header and mobile tab bar. § 2026-09-11 — Shell/IA Stage 1, prompt 2 of 4: the lead detail panel, and why the jump strip won. § 2026-09-11 — Shell/IA Stage 1, prompt 3 of 4: the pipeline card, and why Grouped won. § 2026-09-11 — Shell/IA Stage 1, prompt 4 of 4: the form's container, the Settings layout, and a width check a unit test cannot be. § 2026-09-12 — The lead form at real scale: what actually causes the scrolling, and why width was not it. § 2026-09-12 — Shell/IA Stage 2: the lead read panel and edit drawer, wired at one width. § 2026-09-13 — Shell/IA Stage 2: the Quiet shell wired, and the read panel's way into the edit drawer. § 2026-09-13 — Shell/IA Stage 2: the pipeline card wired, one field block, and a row for the status select, § 2026-09-13 — Shell/IA Stage 2: Settings wired, Variant Split, and the pass closes |
| **`/prospects` column resize + reorder** (1 prompt) | ✅ Complete, verified with a real pointer in the browser (City border 103 → 206px with its body cells following, Business grip dropped onto Rating moving header and body together, a far-left drag stopping at the 88px minimum with zero page overflow). **Opt-in, beside the Table shell, not inside it** — `src/components/ui/table-columns.tsx` over pure state in `src/lib/table/column-layout.ts`; five other tables use the shell and render unchanged. Pointer events with capture, not HTML5 DnD; keyboard-operable grip and resize separator. Auto layout until the first resize, then every column frozen at its measured width under `table-fixed`. **Session-only: width and order reset on reload, and persistence is its own open bullet in `docs/KNOWN_GAPS.md`.** `src/lib/prospects/sort.ts` and sorting untouched. No schema, migration, RLS or grant. `check:widths` now covers `/prospects`. 567/68 on `npm test` | 2026-09-14 | 2026-09-14 — `/prospects` column resize and reorder, session-only |
| **`/login` redesign** (P1's last piece; Stage 1 of 2) | 🟡 **Stage 1 comps built; Variant Split picked 2026-09-15, not yet wired.** Split puts `BRAND.tagline`, `BRAND.description` and an explained demo block in a brand pane beside the form, stacking below `md` with the form first; Centered and Masthead are kept as the record, not maintained. Three dev-only variants at `/shell/login` (`/centered`, `/split`, `/masthead`), all rendering ONE shared `preview/LoginForm.tsx` over the static `preview/mock-login.ts` — so the axis is the frame around the form and, above all, **where the "View demo" way in lives**: a full-width secondary button under the form, its own explained block in a brand pane, or a ghost button in a 48px top bar. Each full-size page links its empty, wrong-password, notice and `?long=1` states. The index shows scaled real previews at 1440 wide, empty and error — **a phone row was built and removed because it lied**: Split stacks at the `md:` VIEWPORT breakpoint, so a 390px frame in a 1440px window drew two squeezed columns (the same fact `/shell/form/full` hit). **Nothing signs anyone in:** the form cancels its submit, and the demo link is `#view-demo`, never `/demo`, which would mint a demo session over the developer's own. Two deliberate differences from the shipped page for the review: fields gain visible labels, and the error banner uses `--danger` rather than the orange pill pair the shipped page uses outside the pill palette's rule. **The fence is a test, not a promise:** `login-fence.test.ts` runs `git diff --stat 47313dd` and an untracked-file listing over `(auth)/login`, `(auth)/signup`, `invite/[token]`, `src/lib/auth/actions.ts` and both middleware files, and must both be empty — Stage 2 updates its base or deletes it deliberately. Gate in every `page.tsx`, not only the layout. `check:widths` covers the three variants at `?long=1` and is clean at both viewports. No schema, migration, RLS, grant, Server Action or query. `docs/ROADMAP.md` P1 stays until the pick is wired | 2026-09-15 | Not yet — no dated addendum; the comps' own comments carry the reasoning |

---

## Known Gaps — Full Historical Record

The text below is the complete, unedited Known Gaps section exactly as it stood in `CLAUDE.md` immediately before the 2026-07-26 restructure that compressed it to one-line dispositions. Kept here verbatim so no historical detail was lost in that rewrite. For the current, up-to-date disposition of each gap, see `CLAUDE.md`'s own Known Gaps section — treat this block as frozen history, not a living document.

**Every item below carries an explicit, dated disposition as of Prompt 15b (2026-07-22) — fixed, or consciously deferred with a stated reason. None of these are silently carried forward; a future prompt should treat an undated bullet here as stale and re-triage it.**

- **The CSV Import/Export Migration Wizard (roadmap Prompts 9–10) was never actually built — confirmed genuinely missing, not just unreachable, during a 2026-07-24 production gaps sweep.** The roadmap's Phase 3 section describes it as part of the 15-phase plan, which reads as if it shipped, but nothing backs that up: no `ImportWizardLayout`, `CsvUploadDropzone`, or `ColumnMappingTable` anywhere in `src/`; `papaparse` was never added to `package.json` (confirmed by direct read); no route, nav link, or button anywhere in the app references an import feature (confirmed by grep for `import`/`wizard`/`csv` case-insensitively across `src/`); and critically, `CLAUDE.md` has a detailed addendum for every other completed prompt from 11 onward (11, 12, 13, 13a, 14, 15a, 15b) but **none for Prompt 9 or 10** — every other shipped prompt in this build left a paper trail, these two didn't. **Went further than the working tree**: searched the full git history for `**/CsvUpload*`, `**/ImportWizard*`, `**/ColumnMapping*` across all branches (`git log --all --diff-filter=A`) — zero hits. Ran `git fsck --unreachable` to check for orphaned/dangling commits that might hold a deleted attempt — found a few (two identical duplicate "Initial commit" trees from what's almost certainly the same "forced graph sync" incident noted in the Prompt 14 addendum, plus a stray index/WIP autostash), diffed their full file trees against the real initial commit, and confirmed byte-for-byte identical, zero CSV/wizard/import files in any of them. This is a genuine build gap, not a rebuild-from-scratch-vs-recover-a-deleted-commit question — there is nothing to recover. **Disposition (2026-07-26): CSV *import* fully built and live-verified; CSV *export* still deliberately open.** Part 1 (upload + column mapping) and Part 2 (Zod validation + tenant-scoped chunked batch insert) are both done — see the Prompt 9 and Prompt 10 addenda. The only remaining piece of the original "Import/Export" phase title is **export**, which was excluded by design: it shares no machinery with the import pipeline (straight `leads` → CSV serialization, nothing to parse, map, or validate) and belongs in its own small follow-up. Historical detail of the original discovery follows. **Superseded disposition (2026-07-25): partially fixed — Part 1 built, Part 2 open.** The "missing" verification was re-run from scratch before building (still zero hits, `papaparse` still absent), confirming it hadn't quietly appeared since. Upload + column mapping now exist and are live at `/import` with a Sidebar entry — see the Prompt 9 addendum above. **Still open: the Zod validation layer and the batch-insert Server Action (Prompt 10)** — today the wizard's "Continue" from the mapping step lands on an explicit placeholder panel stating nothing has been saved, so no partial/misleading import path exists in the meantime. **Also still open, and deliberately so: CSV export**, which is excluded from Prompts 9–10 entirely (see the addendum's scope-boundary note) and needs its own small follow-up.
- **Password reset flow — built and shipped 2026-07-25.** Was genuinely absent (not "magic-link-only by design"), flagged 2026-07-24, built the next day: `/forgot-password` + `/reset-password`, reusing the existing `/auth/confirm` handler. **Disposition (2026-07-25): fixed.** Live-verified end to end with a real disposable mailinator inbox — real email from Supabase Auth, click-through, new password set, login with it succeeded. See the Password Reset Flow addendum above for the full build, including a real bug this surfaced in `/auth/confirm` (it never handled the `code` param a real PKCE-flow link redirects with — fixed alongside this).
- **Signup-confirmation path not live-email-verified after the `/auth/confirm` PKCE fix above.** The fix only *adds* a `code`-handling branch; the pre-existing `token_hash` branch (what signup confirmation was already tested against, in the Prompt 12 addendum) is untouched, and `signUp`/`resetPasswordForEmail` share the identical PKCE-configured SSR client, so it should carry over — but this session's own test volume triggered Supabase's email rate limit before a real signup email could be sent to confirm it live. **Disposition (2026-07-25): consciously deferred, not skipped.** The reasoning above is sound but is reasoning, not a live-verified fact for this specific path. Revisit with one real signup + real email click-through once the rate limit has cleared — cheap to check, shouldn't be left unconfirmed indefinitely given `/auth/confirm` is the single shared gate for every email-based auth flow in this app.
- **`leads.next_action_at` had no edit UI anywhere — P0, found in the 2026-07-25 settings/IA audit, fixed same day.** The "Going Cold" SLA rule is described in Section 1 of this file as a core mechanic, and `next_action_at` is read in 8 places — but was **written** only by the DB default, the webhook ingest path, and the demo seed script. **Disposition (2026-07-25): fixed.** `EditLeadModal` now has a "Follow-up due" `datetime-local` field alongside Status; the local-timezone value is converted to a full ISO string client-side (the browser's real `Date` object knows the user's actual offset, so the server never has to guess a runtime timezone from an offset-less string) and submitted via a hidden field, validated in `updateLead`. Live-verified end to end, not just that it saves: changed an overdue lead's date from the past to a future date, saved, reloaded Today — the lead disappeared from SLA Critical and reappeared in High-Value with the new date/time. See the P0 Fixes addendum below.
- **Archiving a lead was a one-way trip with no in-app recovery — P0, found in the 2026-07-25 audit, fixed same day.** `archiveLead` sets `archived: true` and all read queries filtered `.eq("archived", false)`; the only un-archive path in the codebase was the webhook Resurrection Engine. **Disposition (2026-07-25): fixed.** Contacts now has "Active"/"Archived" filter tabs (`?archived=true`, additive to the existing directory, not a new page); `EditLeadModal` conditionally renders "Unarchive lead" on archived leads, which resets `archived: false` and `status: "NEW"` — matching the webhook Resurrection Engine's own reset behavior — and logs a `SYSTEM_ALERT` for the same audit-trail reason the webhook path does. The existing "Archive lead" button now requires a `window.confirm()` before submitting. Live-verified end to end: archived a lead (confirm-cancel correctly blocked it, confirm-accept archived it), found it under the Archived tab, unarchived it, confirmed it reappeared in Active with status reset to "New" and the SYSTEM_ALERT log entry present in its timeline. See the P0 Fixes addendum below.
- **No account-level (user-scoped) settings surface exists at all — P1/P2, found in the 2026-07-25 audit.** Every settings item in the app is org-scoped; there is no route, panel, action, or column for changing your own password while signed in (P1 — `/reset-password` is reachable when authenticated but is linked from nowhere in-app; the only in-app link is `/forgot-password` from the logged-out login page), changing your email, setting a display name (the `Header` avatar is `userEmail.slice(0,1)`), notification preferences (every OWNER/ADMIN gets weekly + new-lead email unconditionally via `getOwnerAdminRecipients`), or deleting an account. **Disposition (2026-07-25): flagged, not fixed.** See the Settings & Configuration Inventory above for the full table.
- **Team management is view-only beyond invites — P1, found in the 2026-07-25 audit.** `TeamPanel.tsx:26` renders each member's role as plain text; grep confirms no `updateMemberRole` or `removeMember` action exists anywhere. There is no way to promote/demote a member, remove one, or leave an org. Invite create/copy/revoke all work. **Disposition (2026-07-25): flagged, not fixed.** Interacts with the `leads`-role-enforcement gap below — both become real at the same moment (the first genuine MEMBER invite).
- **`organizations.webhook_secret` is documented as rotatable but has no rotation UI or action — P1, found in the 2026-07-25 audit.** Section 2 of this file calls it "a unique, server-generated, rotatable token"; `OrgDetailsPanel` displays and copies the derived URL, and grep for `rotate` across `src/` returns zero. If the secret ever leaks, the only remedy today is a manual SQL update. **Disposition (2026-07-25): flagged, not fixed.**
- **Three of five `organization_credentials` vault columns have no UI, and three `leads` social columns are entirely dead — P2/P3, found in the 2026-07-25 audit.** `ApiKeysPanel` exposes only Gemini and Anthropic; `api_key_openai_secret_id`, `token_resend_secret_id`, and `token_twilio_secret_id` have no form field (consistent with the Prompt 15a finding that none of those providers has a real caller yet). Separately, `social_google_business` / `social_facebook` / `social_instagram` have **zero** references anywhere in `src/` — not in `LEAD_COLUMNS`, no form input, never rendered. **Disposition (2026-07-25): flagged, not fixed.** The credential fields are correctly deferred until each provider has a caller; the three social columns need an actual decision — build the UI or drop the columns — rather than sitting as permanent dead schema.
- **Email case-sensitivity is inconsistent across ingestion paths — found while building CSV import (2026-07-26), flagged rather than silently fixed.** `unique_tenant_client_email` is a plain `TEXT` unique constraint, so Postgres compares it case-sensitively. The **CSV import** path lowercases at the Zod layer (`csv-lead-schema.ts`) and is correct. The **webhook** path does not: `webhook-payload-schema.ts` is `.trim().email()` with no `.toLowerCase()`, and `ingest-lead.ts` looks up the existing lead with `.eq("email", payload.email)` on the raw value. Net effect: a form that submits `Jane@X.com` today creates a *second* lead alongside an existing `jane@x.com` — and worse, an archived lead submitting with different casing won't match, so the Resurrection Engine silently won't fire. The manual `createLead`/`updateLead` actions in `lib/leads/actions.ts` don't lowercase either. **Disposition (2026-07-26): flagged, not fixed.** Deliberately out of scope for the prompt that found it (which was explicitly instructed to flag rather than edit that file). The fix is small — add `.toLowerCase()` to `webhookPayloadSchema.email` and normalize in the two lead actions — but it needs a decision about **existing rows**: any already-stored mixed-case emails would need a backfill (`UPDATE leads SET email = lower(email)`), which can itself collide with the unique constraint if both casings already exist. Check for existing collisions before doing the backfill, not after.
- **`organization_credentials` has no "clear this key" control.** Both the RPC layer (`vault_set_org_credential`) and the settings form treat an empty field submission as "leave unchanged" — there is no way, through the app, to actually remove a configured key/rotate it back to unset (only rotate it to a new value). **Disposition (2026-07-22): consciously deferred.** Confirmed live via SQL that the org has zero real credentials configured today (the sole `organization_credentials` row has all five `*_secret_id` columns null — a harmless leftover from an earlier prompt's test fixture, not a real configured key). Rotation already covers the realistic "wrong key" scenario; full de-configuration has no real operational need yet with a single org and no configured providers. Revisit if a real customer explicitly needs to fully de-provision a key.
- **`leads` CRUD has zero role enforcement** — any MEMBER has full read/write/create parity with OWNER/ADMIN. **Disposition (2026-07-22): consciously deferred.** Confirmed live via SQL: exactly one row exists in `organization_members` today — the OWNER (`admin@tekguyz.com`) — zero MEMBER-role users exist in production. The gap has zero real exposure right now; it's purely theoretical until a second user is actually invited. Revisit before (not after) the first real MEMBER invite goes out, since that's the moment this stops being theoretical.
- **Command palette (Prompt 8) does a full client-side fetch + fuse.js fuzzy match, no pagination or debounce.** **Disposition (2026-07-22): consciously deferred.** Confirmed live via SQL: 0 leads exist in the real org today. Trivially fast at this volume by construction — there's nothing to paginate or debounce yet. Revisit once real contact volume grows large enough to matter (rough threshold: low hundreds of contacts), not on a calendar schedule.
- **Mobile AppShell/Sidebar has no responsive collapse.** Originally deferred (2026-07-22) against a stated trigger: "before any client-facing mobile demo, or before Focus List becomes the primary mobile view in production." **Disposition (2026-07-25): still not built, but the trigger itself is now being reconsidered, not newly discovered.** This isn't a fresh finding — it's the same known gap, with an open question about *when* to address it: likely alongside the upcoming Claude Design pass (a broader visual/UX treatment) rather than as an isolated patch beforehand. Deliberately not building a standalone mobile fix now — doing so ahead of that pass risks solving it twice, once quickly and once properly. Revisit once the Claude Design pass's scope and timing are settled; until then this stays an open, actively-tracked decision rather than a scheduled-but-ignored one.
- **`get_advisors` (security + performance) run fresh via Supabase MCP as part of this triage (2026-07-22).** Findings, triaged:
  - **Leaked Password Protection is confirmed DISABLED** (`auth_leaked_password_protection`, WARN) — this resolves the open question from earlier in the build (it was recommended as a dashboard toggle; whether it was actually flipped was previously unverifiable by any tool). **Disposition (2026-07-22): consciously deferred, not an oversight.** Confirmed with the human: this feature requires a paid Supabase plan, and this project is on the free tier. Not actionable today regardless of intent. Revisit only if/when the project upgrades off the free tier — at that point this advisor warning becomes the trigger to actually flip it, not before.
  - Several `SECURITY DEFINER` functions flagged as callable by `anon`/`authenticated` (`get_invite_preview`, `accept_organization_invite`, `create_organization_with_owner`, `get_org_webhook_secret`, `get_organization_members`, `vault_set_org_credential`) — all **by design**, each documented in section 2's schema notes with its own internal auth/role check (e.g. `get_org_webhook_secret` re-checks OWNER/ADMIN, `vault_set_org_credential` re-checks role via `auth.uid()`). Not a new finding, no action needed. **`vault_set_org_credential` specifically live-tested, not just code-reviewed, on 2026-07-25**: created a genuine throwaway MEMBER-role user, attached to "TEKGUYZ Demo" (never the real org) via a service-role fixture insert, signed in through the real anon-key client, and called `vault_set_org_credential(p_org_id, 'api_key_gemini', ...)` as that authenticated MEMBER session. Result: rejected with `"not authorized"`, exactly matching the function's internal `v_role not in ('OWNER','ADMIN')` check — and independently confirmed via `organization_credentials` that zero rows were written for the org, so the rejection has no side effects either. All test fixtures (user, membership) deleted immediately after, confirmed via SQL (`leftover_users: 0`, `leftover_memberships: 0`). This is the one item in this bullet's list that's now backed by a live adversarial test rather than a code read; the other five remain code-reviewed only.
  - `rls_enabled_no_policy` on `organization_credentials` and `report_sends` — **by design**, same pattern as documented elsewhere in this file: RLS enabled, zero policies, service-role-only access. Not a new finding.
  - Unindexed FKs (5 on `organization_credentials`'s secret-id columns, 1 each on `organization_invites.invited_by` and `organization_members.user_id`) and 3 `auth_rls_initplan` re-evaluation warnings (RLS policies calling `auth.<function>()` instead of `(select auth.<function>())`) — real, valid Postgres/Supabase best-practice suggestions, but **consciously deferred**: at today's real scale (1 org, 1 member, 0 leads — confirmed live), none of these have any measurable performance impact. Revisit if row counts grow into the thousands, per Supabase's own guidance on when these patterns start to matter.
  - 5 "unused index" INFO notices on `leads`/`activity_logs` — expected, not a design mistake: these indexes were built for known query patterns (SLA queue, starred workspace, outcome/revenue reporting) that simply haven't run against real production traffic yet (0 leads today). Not actionable; they'll show usage once real data and usage exist.
- **Prompt 14/15a env-var and Redirect-URL gaps — fully closed, re-confirmed live 2026-07-24 (Production Gaps Sweep), not assumed from prior notes.** The weekly-report cron's `APP_URL` double-slash bug is fixed in both files via the shared `trimTrailingSlash` helper; `NEXT_PUBLIC_SITE_URL` is eliminated (consolidated into `NEXT_PUBLIC_APP_URL`) — **this consolidation is confirmed committed and pushed** (`git show HEAD:src/lib/auth/actions.ts` on `origin/main` shows the `NEXT_PUBLIC_APP_URL` read, commit `4601c4c`; local `HEAD` and `origin/main` confirmed identical via `git fetch` + `git rev-parse`), closing the "still uncommitted" note this entry previously carried. **Re-ran the exact three-variant redirect test (bare origin / with path / with trailing slash) against both local and production fresh, via a temporary self-cleaning `generateLink`+`deleteUser` harness — not an assumption the earlier fix pattern still holds:** production honors all three variants exactly (bare origin, `/auth/confirm`, and `/` all resolve with zero fallback); local honors `/auth/confirm` and `/` exactly, with only the bare-origin-no-path shape falling back to the Site URL default — the same harmless edge case found before, since the app never actually requests a redirect with no path. Local `.env`'s `NEXT_PUBLIC_APP_URL` is confirmed set to `http://localhost:3000` (the human set this after the earlier follow-up flagged it pointing at production) — local dev now self-references instead of silently linking to production.
- **Manual, human-only checklist (cannot be verified by this codebase or any MCP tool):**
  1. If/when this project ever upgrades off Supabase's free tier, enable Leaked Password Protection (Authentication → Policies/Providers) — confirmed still OFF as of 2026-07-24, and confirmed with the human this is a paid-tier-only feature, not an oversight. This is the one item in this whole file that needs a human dashboard action, not a code fix.

---

## Known Gaps — Resolved Items Archive

Living, append-only index — unlike the frozen historical record above, this section keeps growing. Each entry is a Known Gaps item that was fully resolved (✅, no remaining open scope) and relocated out of `CLAUDE.md`'s Known Gaps section on 2026-07-30 to keep that section to open items only. Same one-line format each item had in `CLAUDE.md` — no added narrative here; the full build/verification detail for each still lives in the addendum its own pointer names. Per `CLAUDE.md`'s Session & Verification Discipline, any future Known Gaps item that flips from ⬜ to ✅ gets appended here in the same session it closes, rather than left inline to accumulate.

- **A refused demo write inside a form shows the raw Postgres message inline, not the read-only notice.** ✅ Closed 2026-09-14 — every action that returns a write error as `{ error }` now builds that message through `demoAwareMessage` in `src/lib/demo/demo-aware-error.ts`, which shares one private `42501` + `isDemo` test with `demoAwareError`. Wired in nine files: `leads/actions.ts` (create, edit), `tasks/actions.ts` (create, edit), `actions/prospect-actions.ts` (status, notes, archive), `actions/prospect-promote-actions.ts`, `actions/credentials-actions.ts` (save, clear), `invites/actions.ts` (create, accept), `account/actions.ts` (display name, notifications), `organizations/actions.ts` (settings, webhook rotation), `organizations/team-actions.ts` (role change, removal). The leads and team translators keep their own sentinel messages first, because those RAISEs are `42501` too. The copy moved to `read-only-refusal.ts` and `DemoReadOnlyNotice` reads it, so card and inline text cannot drift. No role, grant, policy or migration touched. Verified in headless Chrome through the real `/demo`: New Lead and a prospect's notes both rendered "This demo is read-only…" inline; the demo owner's duplicate-email New Lead still rendered the raw `unique_tenant_client_email_ci` text. The original bullet, verbatim: ⬜ Registered 2026-09-14 — the "This demo is read-only" notice covers writes that THROW to an error boundary. Actions that RETURN `{ error: message }` into their form (for example New Lead and lead edit in `src/lib/leads/actions.ts`, task edit, prospects, invites, account and API keys) never reach a boundary, so a demo visitor sees text like `permission denied for table leads` under the form. Callers that catch a throw themselves (Kanban drag, note capture, Needs Review) show their own failure text. Left out because the 2026-09-14 prompt was fenced to the refusal path at the boundary. The fix, when wanted, is the same `42501` + `isDemo` test applied where those actions build their returned message. Full history: `docs/ADDENDA_LOG.md` § 2026-09-14 — The demo explains a refused write, § 2026-09-14 — The demo explains a refused write inside a form.

- **Any route under `(app)` without its own `loading.tsx` falls back to the Today agenda's skeleton.** ✅ Closed 2026-09-14 — the per-route fix: `/import` and `/prospects/import` each gained their own `loading.tsx`, both built on a new shared `DropzoneSkeleton` (`src/components/import/DropzoneSkeleton.tsx`) because both pages open on `CsvUploadDropzone`. Those were the only two `(app)` page segments without one; `(app)/loading.tsx` and the Today shape are unchanged and now serve only `/`. `/prospects/import` was in fact falling back to `prospects/loading.tsx` (the call list's table), not the agenda — still the wrong shape. No action, query or schema touched. Verified in headless Chrome at 1440×800 with 4s of emulated latency: `/`→`/import`, `/prospects`→`/prospects/import` and `/`→`/prospects/import` all showed the dashed dropzone skeleton with no three-column grid; a control `/contacts`→`/` still showed the agenda grid. The original bullet, verbatim: ⬜ Registered 2026-08-28 — `src/app/(app)/loading.tsx` is deliberately shaped like `TodayAgenda`'s three-column layout, and Next uses the nearest ancestor boundary, so `/import`, `/prospects/import` and any future segment without its own file show a three-column agenda skeleton that matches nothing on the page. Found while A/B-testing the loading-skeleton flash: deleting the four route-level `loading.tsx` files did not remove the skeleton, it just swapped in the agenda shape (median 381ms vs 345ms), which is what exposed the fallback. The four files were restored. The fix is either a neutral shared `(app)/loading.tsx` with the Today shape moved into its own segment, or a `loading.tsx` per remaining route — a shape decision, not a bug fix, and it overlaps the Shell/IA pass below. Full history: `docs/ADDENDA_LOG.md` § 2026-08-28 — The loading-skeleton flash: a fixed auth tax on every navigation, § 2026-09-14 — Per-route loading skeletons for the two importers.
- **The demo shows an error page, not an explanation, when a write is refused.** ✅ Closed 2026-09-14 — both halves. The warning half shipped 2026-09-04: a neutral `Read-only` badge in the shell header for the demo tenant only (`src/components/shell/DemoModeBadge.tsx`). The refusal half shipped 2026-09-14: a write the `demo_readonly` role refuses now renders "This demo is read-only" instead of the generic error boundary. Every thrown Supabase error in the five write-action files passes through `demoAwareError` (`src/lib/demo/demo-aware-error.ts`), which tags it with the fixed digest `TEKGUYZ_DEMO_READ_ONLY` only when the code is `42501` AND `getCurrentOrg()` says `isDemo`; every other error is returned as the same object. It is a digest because a production build strips a Server Action error down to its digest before the browser sees it. The root, `(app)` and pipeline boundaries render `DemoReadOnlyNotice` on that digest and their old card otherwise. `42501` alone is never enough: real tenants raise it from the leads role trigger, the team-management RPCs and RLS `WITH CHECK`. No role, grant, policy or migration touched. Verified on `next build` + `next start` through the real `/demo`: a real Dismiss click showed the notice; the same click with the task id swapped to `not-a-uuid` (Postgres `22P02`) showed "Something went wrong". Writes that return their error into a form instead of throwing are a separate open bullet. Full history: `docs/ADDENDA_LOG.md` § 2026-09-14 — The demo explains a refused write.
- **Four pages show their name twice — once as the shell header's title and again as their own `<h1>`.** ✅ Closed 2026-09-14 — the page `<h1>` on Reports, Prospects, `/import` (in `ImportWizardLayout`) and `/prospects/import` is now `sr-only`, so each page shows its name once and still exposes exactly one `<h1>` to assistive tech; verified in a real browser accessibility-tree read. No shell, action or schema change. Full history: `docs/ADDENDA_LOG.md` § sr-only page headings: closing the duplicate-title gap (2026-09-14).
- **The Settings webhook caption and the help article still describe the body-only signature.** ✅ Closed 2026-09-14 — the caption in `OrgDetailsPanel.tsx` and the `webhook-setup` article in `src/lib/help/content.ts` now name both headers (`X-TekGuyz-Timestamp`, `X-TekGuyz-Signature`), the `timestamp.body` construction, the five-minute window and the refusal of a reused signature; the article also tells an integrator that a retry must re-sign. Copy only — no component, action or test changed. Operational note that moved here with it: the Upstash keys in `.env` and Vercel point at tekguyz.com's database, so if either copy goes missing or is misnamed, both guards fail open and log `WEBHOOK_STORE_UNAVAILABLE` on every request. Full history: `docs/ADDENDA_LOG.md` § 2026-09-14 — Webhook replay protection and a Redis-backed rate limiter.
- **The webhook signature carries no timestamp or nonce, so a captured request can be replayed.** ✅ Closed in code 2026-09-14 — the signature now covers `${timestamp}.${rawBody}`, the timestamp travels in `X-TekGuyz-Timestamp` and must be within 300s of the server clock either way, and a signature already seen for the tenant is refused through Redis (`${organization_id}:${signature}`, `SET NX EX 600`), all with the same opaque 401. Hard cutover, not dual-supported; deployed back to back with tekguyz.com's caller the same day (CRM `f91b849`, site `3f8dda0`) and confirmed by a real submission. The original bullet, verbatim: ⬜ Registered 2026-08-18 when HMAC signing shipped — the signature covers the body only, so an attacker who captured a valid signed request over a compromised transport could resend those exact bytes and have them accepted. Deliberately not built now: the blast radius is a re-submitted enquiry that already arrived, and repeat enquiries collapse onto the same lead rather than creating duplicates, so a replay adds a submission row rather than corrupting anything. The fix when it is warranted is a timestamped signature (sign `timestamp.body`, send the timestamp in its own header, reject a skew beyond a few minutes) — a breaking protocol change for every caller, which is why it is not being bundled in. Full history: `docs/ADDENDA_LOG.md` § 2026-08-18 — HMAC request signing replaces the URL-path webhook secret, § 2026-09-14 — Webhook replay protection and a Redis-backed rate limiter.
- **Shell/IA needs a dedicated redesign pass, not incremental patches.** ✅ Fully closed 2026-09-13 — registered 2026-08-27 from five shell complaints and widened 2026-08-28 to an aesthetic and IA brief. Stage 1 built dev-only comps and picked a direction for each surface: the shell's Variant C — Quiet, the detail panel's Variant A — Jump strip, the pipeline card's Variant Grouped, the form container's Variant Drawer, and Settings' Variant Split. Stage 2 wired all five into production: the lead read panel and edit drawer at one width (2026-09-12), the Quiet shell and the read panel's Edit entry point (2026-09-13), the Grouped pipeline card with one shared field block (2026-09-13), and `/settings` as four Split rows over the four real panels (2026-09-13). No schema, no migration, no RLS and no grant in any Stage 2 prompt; every form's field-parity diff was run and empty. The org switcher stayed out of scope and keeps its own open bullet; the duplicate `<h1>` on Reports, Prospects and both importers was spun out as its own open bullet. Full history: `docs/ADDENDA_LOG.md` § 2026-09-07 — Shell/IA Stage 1, prompt 1 of 4: the sidebar, header and mobile tab bar, § 2026-09-12 — Shell/IA Stage 2: the lead read panel and edit drawer, wired at one width, § 2026-09-13 — Shell/IA Stage 2: the Quiet shell wired, and the read panel's way into the edit drawer, § 2026-09-13 — Shell/IA Stage 2: the pipeline card wired, one field block, and a row for the status select, § 2026-09-13 — Shell/IA Stage 2: Settings wired, Variant Split, and the pass closes.
- **The command palette does not search prospects.** ✅ Fully closed 2026-09-05 — the bullet was registered 2026-08-27 (palette searched Contacts only), half-closed 2026-08-28 when Tasks was added, and is now closed by a third source. `fetchSearchableProspects` (in `src/lib/actions/prospect-actions.ts`) wraps a new `searchProspectsForOrg` in `src/lib/prospects/queries.ts` — a narrow six-column select, `archived = false`, tenant-scoped by the **existing** "Members read tenant prospects" RLS policy. **No migration, no policy, no grant**: this is a read through the same authenticated client every other prospects query uses, not a new access pattern. The palette renders a third labelled group with the same no-empty-header degrade the other two have, and arrow keys run continuously across all three. Prospects have no profile sheet — `/prospects` is a flat table with no per-record detail view — so selecting one pushes `/prospects?highlight=<id>`; the page reads that param and hands it to `ProspectsTable`. The arrival highlight itself was **extracted, not copied**: `src/lib/hooks/use-arrival-highlight.ts` is now the one implementation, and `TasksSection` consumes it too. Related and still open: the pagination/debounce bullet in `docs/KNOWN_GAPS.md`, deliberately left there. Full history: `docs/ADDENDA_LOG.md` § 2026-09-05 — Command palette: prospects as the third source.
- **CSV Import/Export Wizard (Prompts 9–10).** ✅ Closed 2026-09-05 — Import shipped long before (re-verified end to end on 2026-08-15, not 2026-07-26; the chunk write was structurally incapable of inserting a row from the day `unique_tenant_client_email_ci` landed until the `import_leads_chunk` RPC replaced it). Export shipped 2026-09-05 as `GET /api/leads/export`, a route handler rather than a Server Action so `Content-Disposition` hands the browser a real file; its column set **is** `LEAD_COLUMNS` split on commas, never a hand-written list, so a future column joins the export automatically. Read-only — no schema, no migration, no RLS, no grant — and the auth check is written into the handler because `updateSession` deliberately exempts `/api/*` from its redirect. RFC 4180 quoting plus a leading apostrophe on `=`/`+`/`-`/`@`, because `client_name` is attacker-supplied through the public webhook and a formula in a CSV executes when an operator opens it. Live-verified through the real route: 200, `text/csv`, `tekguyz-demo-leads-2026-09-05.csv`, 20 rows, quoted addresses. Full history: `docs/ADDENDA_LOG.md` § Prompt 9 addendum, § Prompt 10 addendum, § Production Gaps Sweep addendum, § CSV import chunk-write RPC, § 2026-09-05 — Three registered gaps closed: leads CSV export, the `/reports` period filter, one dead declaration.
- **`input:focus-visible`'s `border-color: var(--accent)` is dead CSS.** ✅ Closed 2026-09-05 by **deleting the declaration**, not by reordering layers. `Input`/`Textarea`/`Select` each carry a `border-hairline` utility, and utilities sit in a later cascade layer than `@layer base`, so that one line never painted a pixel; the paired 1px `box-shadow` accent ring was always the thing carrying the accessibility floor. Verified live rather than reasoned: with transitions suppressed and after a real key press (both documented measurement traps — `transition-colors` returns the start value on a same-task read, and Chromium only matches `:focus-visible` on a programmatic focus once real keyboard input has happened), a focused field reads `box-shadow: <accent> 0 0 0 1px` while `border-top-color` is the hairline both before and after focus, and the shipped rule now reads `textarea:focus-visible { box-shadow: 0 0 0 1px var(--accent); outline: none; }`. No visual regression, because nothing visual was ever there. Full history: `docs/ADDENDA_LOG.md` § 2026-09-05 — Three registered gaps closed: leads CSV export, the `/reports` period filter, one dead declaration.
- **Every uncontrolled form posting to a Server Action loses what the user typed when that action returns an error.** ✅ Closed 2026-08-26 — seven files converted to the plain `useState` pattern `PromoteProspectModal` already shipped, every `name=` unchanged so each action's `formData` shape is identical, and no Server Action, schema, migration or RLS policy touched. `EditLeadModal` needed **all five** of its sibling field groups, not just `IdentityFields`: it is one `<form>` split across five files and `form.reset()` does not respect that boundary — its regression test therefore asserts the whole form at once. Controlling a field turned out to be only half the fix, and the other half was found in a browser rather than a test: React does **not** restore a controlled `<select>` after the reset (measured live — React's props read `"WON"` while the DOM read `""`), and Radix's Checkbox actively restores its own mount-time value via its own `reset` listener. Both are handled by one shared hook, `src/lib/forms/use-form-reset-restore.ts`, which restores in a microtask so it cannot depend on listener order. The first version of the tests called `rerender()` after the reset and passed while the bug was still live; they now use `form.reset()` + `waitFor` with no re-render, and all nine were re-proven to fail against the original code. 211/34 on `npm test` (from 179/27), build green, lint and tsc clean, and both forms confirmed live against a real `unique_tenant_client_email_ci` failure through `GET /api/dev-login`. One invite row created while testing was removed and `npm run check:residue` re-run clean. Full history: `docs/ADDENDA_LOG.md` § 2026-08-26 — Controlled fields: closing the React 19 form-reset data loss.
- **There is no way to see, work or promote a prospect — only to import one.** ✅ Closed 2026-08-26 — Prompt 2 shipped `/prospects` (sortable, filterable, Active/Archived), inline `status`/`notes`/`archived` editing, `possible_duplicate_lead_id` surfaced as a link that never blocks, and a Promote action. Migration `20260826130000_prospects_promotion.sql` adds `notes` and `promoted_lead_id` plus one partial index; **no `leads` DDL, no RLS change, no new policy** — `prospects`' existing policies have no column-level gate, and the missing DELETE grant was re-confirmed live after applying. Promote calls `insertLeadWithSubmission`, the function extracted from `createLead` so both paths share one lead-write path rather than reimplementing it. `promoted_lead_id`, never the status string, is the only source of truth for "already promoted"; it and `status = 'CONVERTED'` are written by one statement carrying `where promoted_lead_id is null`, so a second promotion affects zero rows. `SECONDARY_NAV`'s "Prospects" entry now points at the list, with the importer reached by a button on it. 179/27 on `npm test`, 76/76 on `npm run test:rls` (64 pre-existing unchanged), build green, lint clean, and a real promotion driven through the UI with the resulting `leads` and `lead_submissions` rows read back from the database. The one residual case — a lost race orphaning a lead — is its own open bullet in `docs/KNOWN_GAPS.md`. Full history: `docs/ADDENDA_LOG.md` § 2026-08-26 — Prospect promotion: the write path into `leads`.
- **The 122 imported prospects live in `TEKGUYZ Demo`, not `TEKGUYZ`.** ✅ Closed 2026-08-26 — the owner signed in as themselves and re-ran both leadgen CSVs through `/prospects/import`, then ran the scoped `DELETE` in Supabase Studio, because the agent sandbox correctly refused a write against a `public`-schema table (CLAUDE.md § Test-Data Cleanup). Re-verified by the agent against the live database rather than taken on report: `TEKGUYZ` holds **122 prospects, 122 distinct `place_id`s, 117 with a usable `phone_digits` and 5 without, 0 flagged**, split **91 Fort Lauderdale, FL / 31 Fort Worth, TX** — matching the two source files exactly, all at `status = 'NEW'`. `TEKGUYZ Demo` holds **0 prospects**, and its **20 leads are untouched** (21 leads project-wide, unchanged all day). Only the two real organizations exist; no throwaway org or `rls-place-*` fixture survived. `npm run check:residue` clean. The 91-row file legitimately carries 12 distinct `run_id`s summing to 91 — that export is a merge of several scrape runs, not a duplication. The "5 rows have no usable phone number" notice the owner saw is the CSV, not a defect: Google lists no number for those 5. Full history: `docs/ADDENDA_LOG.md` § 2026-08-26 — `prospects`: cold-outreach staging, its RLS and its CSV import.
- **The webhook secret is a bearer credential carried in the URL path, so it lands in request logs.** ✅ Fixed 2026-08-18 — the route is now `POST /api/v1/triage/<organization_id>` and authenticates with an `X-TekGuyz-Signature` header holding a hex HMAC-SHA256 of the **raw, unparsed** request body, keyed by `webhook_secret`, compared with `crypto.timingSafeEqual`. The secret never leaves either end, so it can no longer be logged; the org id in the URL grants nothing on its own and an unsigned request for a real org gets the same opaque 401 as one for an org that does not exist. No schema change — the column keeps its rotation flow, which now rotates the signing key and leaves the endpoint URL unchanged. The old URL-path-secret scheme was **removed, not dual-supported**, and confirmed rejected live. Proven through the real route with 11 cases (wrong key, replayed signature on a tampered body, re-serialized body, unknown org id, the old scheme, and the two accepted paths), test lead removed in the same run. The one real caller, tekguyz.com's contact form, was updated in the same session in its own sibling repo (`C:/Projects/tekguyz-site`, `app/actions/contact.ts`) and proven end to end through the real form in a browser — accepted with the right secret, 401 with a wrong one, test lead removed. Both repos were deployed to production on 2026-08-19 (CRM `c780a21`, site `44c7295`) and confirmed with a genuine tekguyz.com/contact submission that reached the CRM. Full history: `docs/ADDENDA_LOG.md` § 2026-08-18 — HMAC request signing replaces the URL-path webhook secret, § Webhook secret exposure audit.
- **Team management is view-only beyond invites.** ✅ Closed 2026-08-18 — migration `20260818130000_team_management_rpcs.sql` adds `change_member_role` and `remove_organization_member`, both `SECURITY DEFINER` and both re-resolving the caller's own role for the org id they are passed via a new `private.caller_role_in_org(uuid)` helper that has no client grant. No new RLS policy: `authenticated` holds only SELECT and INSERT on `organization_members`, so the RPC-only path is enforced below RLS. The last OWNER cannot be demoted or removed (self included), an ADMIN cannot manage or promote to OWNER, and a MEMBER can only remove themselves; removal releases the leaver's `leads.assigned_to` in the same transaction. Applied to the real project and confirmed live; every rule proven by an actually attempted violation asserting its own sentinel, `npm run test:rls` 43/43. The one residual case — a membership row deleted outside the RPC — is its own open bullet in `docs/KNOWN_GAPS.md`. Full history: `docs/ADDENDA_LOG.md` § 2026-08-18 — Team management: role change and member removal, § Settings & Configuration Inventory.
- **`leads` has no `assigned_to` column and no per-row ownership concept anywhere in the schema.** ✅ Closed 2026-08-18 — migration `20260818120000_leads_assigned_to.sql` adds a nullable `assigned_to uuid` referencing `auth.users(id) ON DELETE SET NULL`, a partial index, and a `BEFORE INSERT OR UPDATE` trigger that rejects assigning a lead outside its own organization; the picker, the card label and a "My leads" filter ship with it. Applied to the real project and confirmed live; the cross-tenant rejection was actually triggered and observed, service-role included, by `npm run test:rls` (26/26). Visibility deliberately unchanged — the two things this bullet also named, member-removal cleanup and "a MEMBER sees only their own leads", are now their own open bullets in `docs/KNOWN_GAPS.md`. Full history: `docs/ADDENDA_LOG.md` § 2026-08-18 — `leads.assigned_to`: per-lead ownership.
- **Live test residue in `TEKGUYZ Demo`: two MEMBER membership rows and two ACCEPTED invites.** ✅ Closed 2026-08-18 — the human ran the `DELETE` in Supabase Studio and confirmed the two backing `auth.users` rows are gone; `npm run check:residue` (handoff check 12) then re-read the live database and reported clean: 1 membership row, the seeded demo OWNER only, and no invite rows in that org. The same SELECT-only script found the residue and printed the removal SQL — it never wrote anything, per CLAUDE.md § Test-Data Cleanup. Full history: `docs/ADDENDA_LOG.md` § 2026-08-18 — Demo-org test residue removed, and the check that confirmed it, § Accepted invites stuck at PENDING — Settings → Team.
- **`src/app/opengraph-image.tsx` reads Inter from `node_modules` at request time.** ✅ Fixed 2026-08-17 by `6a9b204` — both `.woff` files vendored into `public/fonts/`, which is always deployed, and `@fontsource/inter` moved to `devDependencies` where the bundle README always said it belonged. `/opengraph-image` renders byte-identically (same SHA256), so nothing visual changed; what changed is that a pruned or relocated `node_modules` can no longer throw on the first production OG request. Re-verified against the live tree during the 2026-08-18 handoff audit. Full history: `docs/ADDENDA_LOG.md` § 2026-08-17 — OG route: Inter vendored out of `node_modules`.
- **The restricted lead controls are still rendered to a MEMBER.** ✅ Fixed 2026-08-17 by `94de14b` — `ArchiveControls` and `OutcomeFields` now hide entirely for a MEMBER via `canEditLeadLifecycle()` in the new `src/lib/organizations/roles.ts`, so the rule is no longer discoverable only by clicking Archive and reading an error toast. `trigger_enforce_lead_role_restrictions` is untouched and remains the actual boundary — this is presentation only. The hidden path deliberately re-sends the current `outcome`/`actual_revenue`/`closed_at` values rather than omitting the inputs, because `updateLead` writes all three on every save and omitting them would post `null` and trip the trigger. Full history: `docs/ADDENDA_LOG.md` § 2026-08-17 — Deterministic org resolution, and hiding lifecycle controls from a MEMBER.
- **Nothing reconciles an invite whose membership was granted outside `accept_organization_invite`.** ✅ Fixed 2026-08-17 by `supabase/migrations/20260817150000_close_invite_on_member_insert.sql` — an `AFTER INSERT` trigger on `organization_members` now closes any `PENDING` invite matching on `(organization_id, lower(email))`, whatever wrote the membership row, so a hand-run Studio `INSERT` or a service-role script can no longer strand an invite on `unique_pending_invite_per_org_email`. Deliberately still no expiry sweep — invites nobody ever accepts are a different problem and the UI already surfaces them as expired. Live-verified by reproducing the 2026-07-25 out-of-band insert with a disposable service-role script. Full history: `docs/ADDENDA_LOG.md` § Accepted invites stuck at PENDING — Settings → Team, § 2026-08-17 — Closing a stranded PENDING invite on membership insert.
- **The sidebar collapse animates `width` on a flex sibling, and it janks on the Pipeline board.** ✅ Fixed 2026-08-17 — the rail is now absolutely positioned and animates `translate`, with a non-transitioned in-flow spacer holding the content column's leading offset, so no layout runs during the transition. Re-measured like-for-like on `/pipeline` in one session: old implementation dropped frames on every collapse (max 33.4ms and 33.2ms across two runs), new implementation held 60fps with zero frames over 20ms (max 17.1ms and 16.9ms). Applied globally, never per route. Full history: `docs/ADDENDA_LOG.md` § 2026-08-17 — sidebar collapse: a transform-based overlaid rail.
- **Nothing tests that a focus ring actually paints.** ✅ Closed 2026-08-17 — `src/components/ui/dropdown-menu.test.tsx` (5 tests) asserts that no interactive dropdown row carries `outline-none`, `outline-0`, `focus:outline-none` or `focus-visible:outline-none`, covering `DropdownMenuItem` (both variants), `DropdownMenuRadioItem`, a caller-supplied `className`, and a role-based sweep that catches row types added later. Proven to fail before being trusted: re-adding `outline-none` to `DropdownMenuItem` fails 4 of the 5. A real *paint* assertion remains out of reach — no browser-based runner is installed — and that limitation is written into the test file itself rather than papered over. Full history: `docs/ADDENDA_LOG.md` § 2026-08-17 — a regression test for the focus-ring floor.
- **`OptionRow` signals its selected row with background colour only.** ✅ Fixed 2026-08-17 — the selected row now also paints a leading `--accent` marker bar using `NavItem`'s exact `MARKERS.row` geometry, with `relative` unconditional so the pseudo-element always has its positioning context; `bg-canvas-soft` stays as reinforcement. An outline was ruled out rather than overlooked — the row is deliberately non-focusable, so the global `:focus-visible` rule can never reach it. Covered by a new `src/components/ui/OptionRow.test.tsx` (5 tests). Full history: `docs/ADDENDA_LOG.md` § 2026-08-17 — `OptionRow`: a leading marker bar, not a tint alone.
- **Webhook ingestion upserts by email, overwriting the earlier enquiry.** ✅ Fixed 2026-08-17 — `ingestWebhookLead` no longer writes `client_name`/`phone`/`company`/`website`/`physical_address`/`service_category`/`lead_source` on a resubmission; those are first-known values on `leads`, and each enquiry gets its own immutable `lead_submissions` row. Live-verified through the real webhook route: three enquiries at one address, one `leadId`, identity unchanged, three submission rows, Resurrection Engine still firing. Full history: `docs/ADDENDA_LOG.md` § 2026-08-16 — Wave decisions, § 2026-08-17 — `lead_submissions`: the immutable enquiry log.
- **`leads` has no `message` column — the visitor's own words are not first-class data.** ✅ Fixed 2026-08-17, and deliberately **not** by adding that column — `lead_submissions.message` is the home, so the enquiry text is a real column with a UI (`EnquiryHistory` in the profile sheet) and the Spam Shield scores it directly instead of re-parsing `activity_logs.content` JSON. Avoids the `EditLeadModal` field-parity plumbing a `leads.message` column would have dragged in. Full history: `docs/ADDENDA_LOG.md` § 2026-08-17 — `lead_submissions`: the immutable enquiry log.
- **10 pre-existing shield-archived leads are still hidden.** ✅ Closed 2026-08-16 as **moot, not fixed** — the backfill was finally attempted and the rows no longer exist: the real `TEKGUYZ` org (`95c1bc71-2645-4e35-a9f6-078993f1c586`, resolved live via Supabase MCP) holds 1 lead and 0 archived, and the whole project has zero archived leads across both orgs; `510c28db`, `7311d3e2` and `4c049981` all return no row. The "10" was a 2026-08-11 point-in-time figure, since removed by the test-data cleanup discipline. No `UPDATE` was run. Full history: `docs/ADDENDA_LOG.md` § Spam Shield routing fix, § 2026-08-16 — Wave decisions.
- **The global `:focus-visible` outline never paints in `--accent`.** ✅ Closed 2026-08-16 by the shell close-out — **mostly withdrawn as mis-measured, and the one real part fixed.** The app-wide claim does not reproduce: measured with a real Tab and one element per read, every focusable shell control already resolves to `outline: 2px solid var(--accent); outline-offset: 2px`, confirmed visually in both themes and on the 56px rail. The original currentColor readings came from two artifacts — `transition-colors` transitions `outline-color`, so a same-task read returns the start value, and `getComputedStyle` lags one element behind in a focus loop. What *was* real: `DropdownMenuItem` carried shadcn's `outline-none`, deleting the rule for all five identity-menu rows; that class is now removed from the primitive. Full history: `docs/ADDENDA_LOG.md` § Shell redesign close-out.
- **Mobile AppShell/Sidebar has no responsive collapse.** ✅ Fixed 2026-08-16 by the shell redesign — below `md` the sidebar is not displayed in either collapse state and navigation is a bottom tab bar (Today / Pipeline / Contacts / More) with a "More" sheet for secondary items; a manual, cookie-persisted desktop collapse to a 56px icon rail was added in the same pass. Deferred since 2026-07-25 on the stated trigger "before Focus List becomes the primary mobile view"; that trigger fired. Full history: `docs/ADDENDA_LOG.md` § Application shell redesign.
- **`Badge`'s `cold` tone fails WCAG AA and is therefore unused.** ✅ Fixed 2026-08-17 by adding a `--cold-fg` token and repointing the tone at it — **4.58:1 light / 4.84:1 dark**, up from 2.72:1 / 2.35:1, with `--cold` itself untouched in its dashed-border role. Full history: `docs/ADDENDA_LOG.md` § `--cold-fg`: making `Badge`'s cold tone AA-readable.
- **`CommandResultItem.tsx` is a raw `<button>` outside a primitive.** ✅ Fixed 2026-08-16 by the shell redesign — it became a thin mapper over a new `ui/OptionRow.tsx` (`div role="option"`), and `CommandBar` became a real combobox/listbox with `aria-activedescendant`. Button was correctly *not* the answer: the rows must not be focusable at all. Full history: `docs/ADDENDA_LOG.md` § Application shell redesign.
- **Password reset flow.** ✅ Fixed 2026-07-25 — `/forgot-password` + `/reset-password`, live-verified end to end with a real email. Full history: `docs/ADDENDA_LOG.md` § Password Reset Flow addendum.
- **`leads.next_action_at` had no edit UI (P0).** ✅ Fixed 2026-07-25, live-verified (an overdue lead correctly moved out of SLA Critical after editing). Full history: `docs/ADDENDA_LOG.md` § P0 Fixes & Password Visibility addendum.
- **Archiving a lead was a one-way trip with no recovery (P0).** ✅ Fixed 2026-07-25 — Contacts Active/Archived tabs + Unarchive, live-verified. Full history: `docs/ADDENDA_LOG.md` § P0 Fixes & Password Visibility addendum.
- **`organizations.webhook_secret` rotation.** ✅ Fixed 2026-07-27 — "Rotate webhook secret" in `OrgDetailsPanel`, live-verified. Full history: `docs/ADDENDA_LOG.md` § Webhook Rotation, Clear-Key, and Locale Options addendum.
- **`physical_address` had no edit UI; 3 `leads` social columns were entirely dead.** ✅ Fixed 2026-07-27 — text-input UI in `EditLeadModal`, all four columns moved onto the base `Lead` type; `ai_brief` generation deliberately still out of scope as its own AI follow-up. Full history: `docs/ADDENDA_LOG.md` § Settings & Configuration Inventory, § Lead Field Completion addendum.
- **Timezone/currency option lists and `formatCurrency` locale.** ✅ Fixed 2026-07-27 — curated lists in a shared `src/lib/organizations/org-options.ts`, per-currency default locale. Full history: `docs/ADDENDA_LOG.md` § Webhook Rotation, Clear-Key, and Locale Options addendum.
- **Email case-sensitivity across ingestion paths.** ✅ Fixed 2026-07-26 — all four write paths normalize to lowercase, backed by the `unique_tenant_client_email_ci` index, live-verified. Full history: `docs/ADDENDA_LOG.md` § Prompt 10 addendum, § Email Case-Insensitivity: Full Fix addendum.
- **`organization_credentials` has no "clear this key" control.** ✅ Fixed 2026-07-27 — `vault_clear_org_credential` RPC, adversarially live-tested against a real MEMBER session. Full history: `docs/ADDENDA_LOG.md` § Webhook Rotation, Clear-Key, and Locale Options addendum, § Account Panel: Password, Display Name, Notification Preferences addendum.
- **`src/lib/leads/actions.ts` was 219 lines, over the 200-line cap.** ✅ Fixed 2026-07-28 — `archiveLead`/`unarchiveLead` extracted to a sibling `src/lib/leads/archive-actions.ts`. Full history: `docs/ADDENDA_LOG.md` § Task/Calendar addendum — Prompt 5.
- **`EditLeadModal`'s archive/unarchive handlers had no `catch`.** ✅ Fixed 2026-07-28 — both catch, log, and surface a retry toast with the dialog left open. Full history: `docs/ADDENDA_LOG.md` § Task/Calendar addendum — Prompt 5.
- **`src/components/leads/EditLeadModal.tsx` was 310 lines, over the 200-line cap.** ✅ Fixed 2026-07-28 — split by concern into five siblings under `src/components/leads/edit-modal/`, shell down to 86 lines. Full history: `docs/ADDENDA_LOG.md` § EditLeadModal split addendum.
- **`updateLead` silently NULLed `website`/`lead_source`/`service_category` on every save.** ✅ Fixed 2026-07-30 — inputs added to `IdentityFields.tsx` and `CreateLeadModal`; now covered by `CLAUDE.md`'s Form/Action Field Parity rule. Full history: `docs/ADDENDA_LOG.md` § Silent NULL-on-save data-loss bug.
- **CSV import can never insert a row — the upsert's `onConflict` did not match any index.** ✅ Fixed 2026-08-15 — the chunk write moved into the `import_leads_chunk` `SECURITY DEFINER` RPC, which can infer the expression index PostgREST could not address; live-verified end to end through the real wizard, including the cross-tenant refusal and the failed-chunk branch. Deleted outright from `docs/KNOWN_GAPS.md` rather than left inline as ✅. Full history: `docs/ADDENDA_LOG.md` § CSV import chunk-write RPC.
- **The Table shell has no consumer.** ✅ Closed 2026-08-15 by Prompt 2b — `ColumnMappingTable` and `ValidationResultsTable` now consume `Table`/`TableHead`/`TableBody`/`TableRow`/`TableHeaderCell`/`TableCell`. Still bare on purpose: no sorting, selection or virtualisation, which belong with Table View. Full history: `docs/ADDENDA_LOG.md` § Design System v2 — Prompt 2b.
- **`NeedsReviewQueue` was left on one-off classes by Prompt 2a's scope fence.** ✅ Closed 2026-08-15 by Prompt 2b — rows are `Card`, the count is `Badge tone="orange"`, the dismissal is `Button variant="secondary"`; count badge, verbatim reason text, `?leadId=` deep link and "Not spam" all demonstrated live against a real flagged lead. Full history: `docs/ADDENDA_LOG.md` § Design System v2 — Prompt 2b.
- **Most views still do not consume the v2 primitives.** ✅ Fully closed 2026-08-15 by Prompt 2c — Settings (all four panels plus the three invite siblings), the Help drawer + inline tooltips, `CommandBar` and `CreateLeadModal`. Every **signed-in** view now consumes `src/components/ui/`. **(Corrected 2026-08-16: this line previously read "every view in the app," which the primitive audit measured as false — the five `(auth)` pages, `invite/[token]`, `AcceptInviteButton` and the four error boundaries hold 13 raw controls still on v1 `shadow-elevation-1`. Prompts 2a/2b/2c only ever fenced in signed-in surfaces. See § Primitive audit; the remaining scope is open in `docs/KNOWN_GAPS.md`.)** Of the three primitive gaps Prompt 2c reported, two closed on 2026-08-16 (`Checkbox`, `Button asChild`) and the third (`CommandResultItem`) closed later the same day — see § Application shell redesign. Full history: `docs/ADDENDA_LOG.md` § Design System v2 — Prompt 2c, § Primitive audit, § Application shell redesign.
- **`HelpTrigger` is still on one-off classes.** ✅ Closed 2026-08-15 by Prompt 2c — now `Button variant="secondary" className="w-8 px-0"`, the same treatment as `ThemeToggle` and sign-out. **(State as of 2026-08-15 only — superseded by § Application shell redesign: `HelpTrigger.tsx` and `ThemeToggle.tsx` were both deleted on 2026-08-16 when Help, theme and sign-out moved into the identity menu. Neither file exists.)** Parity measured rather than eyeballed: identical bounding box and identical computed background, colour, border colour, border width and radius in both themes. Full history: `docs/ADDENDA_LOG.md` § Design System v2 — Prompt 2c.
- **Prompt 14/15a env-var and Redirect-URL gaps.** ✅ Fully closed, re-confirmed live 2026-07-24. Full history: `docs/ADDENDA_LOG.md` § Production Gaps Sweep addendum.
- **`updateOrgSettings` had the same silent-RLS-no-op shape `rotateWebhookSecret` had before its `.select().single()` fix.** ✅ Fixed 2026-07-30 — `.select("id").single()` chained, PGRST116 surfaced as a real error; both directions live-verified. Full history: `docs/ADDENDA_LOG.md` § updateOrgSettings silent-RLS-no-op fix.
- **`leads` CRUD has zero role enforcement — any MEMBER has full CRUD parity with OWNER/ADMIN.** ✅ Fixed 2026-08-14 — `archived`, `outcome`, `actual_revenue` and `closed_at` are now OWNER/ADMIN-only on UPDATE, enforced by a `BEFORE UPDATE` trigger, with a live three-role Vitest suite (`npm run test:rls`). Everything else on `leads` stays MEMBER-writable by design. Two narrower successors are open in `docs/KNOWN_GAPS.md` (the controls are still rendered to a MEMBER; there is still no `assigned_to`). Full history: `docs/ADDENDA_LOG.md` § Leads MEMBER-role enforcement addendum.
- **`--accent` has never been visually confirmed.** ✅ Closed 2026-08-14 — the gap asked for a real reference to sample; the CRM's new brand mark became it. Shipped `oklch(0.53 0.181 263.2)` light / `oklch(0.70 0.155 263.2)` dark, hue and chroma locked to the logo blue `#3B6FE0` with lightness lowered to clear AA for the inline-link role. Confirmed live in both `/design` panes: resolves to exactly `#3063D3` / `#6A9BFE`, all accent pairings ≥5.1:1. Full history: `docs/ADDENDA_LOG.md` § Brand identity + `--accent` sampling.
- **Lockup SVGs were built with a stand-in font.** ✅ Closed 2026-08-14 — never shipped in that state. `build_brand.py` was re-run in-repo against real Inter from `@fontsource/inter` (name table confirmed `Inter` Bold / `Inter Medium`); the pipeline has no font-substitution fallback, it skips lockups outright when no font is supplied. All four lockups emitted with the wordmark outlined to two `<path>` elements and zero `<text>`. Full history: `docs/ADDENDA_LOG.md` § Brand identity + `--accent` sampling.
- **The agency mark may still be referenced somewhere in `src/`.** ✅ Closed 2026-08-14 — audited; no component referenced a logo asset at all, so nothing needed repointing. The real exposure was three App Router file conventions serving the old mark — `src/app/favicon.ico`, `src/app/icon.png` and `src/app/apple-icon.png` — all three deleted. The bundle's brief named only `favicon.ico`; `icon.png` and `apple-icon.png` override `metadata.icons` the same silent way. Full history: `docs/ADDENDA_LOG.md` § Brand identity + `--accent` sampling.
- **OG card tagline is unreviewed copy.** ✅ Closed 2026-08-15 — "Every lead, one pipeline." approved by the owner. Now single-sourced in `src/lib/brand/copy.ts` alongside the product name and description, so the card, the manifest and the page metadata cannot drift apart. Full history: `docs/ADDENDA_LOG.md` § Brand application pass.
- **No brand SVG has been composited into a screenshot.** ✅ Closed 2026-08-15 — the owner signed off on the mark as implemented. Resolved across three narrowings rather than one check: the geometry was first proven by pixel-diffing `icon.svg` (browser SVG engine) against the Pillow-generated `icon-512.png`, 0.34% of pixels differing grossly and aspect ratios within 0.5%, so there is no renderer drift between the pipeline and a real browser; then the Browser pane's non-compositing problem cleared, making real screenshots possible; then the mark was reviewed in the contexts that actually matter — the favicon chosen from a rendered 16/32px mockup against both a light and a dark tab, and the auth-screen mark and OG card screenshotted in both themes. Full history: `docs/ADDENDA_LOG.md` § Brand identity + `--accent` sampling, § Brand application pass.
- **OG card wordmark/tagline spacing.** ✅ Closed 2026-08-15 — the gap was 69px against a 44px icon gap, so the tagline read as detached rather than subordinate; now 41px. Relocated out of `docs/KNOWN_GAPS.md` on 2026-08-16, having been left inline past its close date. Full history: `docs/ADDENDA_LOG.md` § Metadata & doc cleanup pass.
- **There is no `Checkbox` primitive in `src/components/ui/`.** ✅ Closed 2026-08-16 — built on Radix with every state derived from an existing primitive, and `AccountPanel`'s two notification checkboxes swapped onto it; both themes verified against real computed styles. Full history: `docs/ADDENDA_LOG.md` § Primitive audit.
- **`Button` has no `asChild` escape hatch, so link-shaped controls restate its classes.** ✅ Closed 2026-08-16 — `asChild` added on `@radix-ui/react-slot`, and all three class copies collapsed (`AccountPanel`'s `LINK_BUTTON_CLASS` deleted; `AlertDialogAction`/`AlertDialogCancel` now compose `Button`). Full history: `docs/ADDENDA_LOG.md` § Primitive audit.
- **The pre-auth `(auth)` route group and the four error boundaries still ran v1 chrome.** ✅ Closed 2026-08-16 — all five `(auth)` pages, `invite/[token]`, `AcceptInviteButton` and all four boundaries now consume `Button`/`Input`/`Card`; raw-element grep 34 hits/23 files → 19/15, `text-canvas-pure` on `bg-accent` → 0, `shadow-elevation-1` → 0 in application code. Both themes verified against real computed styles. Full history: `docs/ADDENDA_LOG.md` § Design System v2 — pre-auth surfaces.
- **Two raw `<input type="checkbox">` remained outside `AccountPanel`.** ✅ Closed 2026-08-16 — `PipelineFields`' "Starred" and `TasksSection`' per-task toggle both on the `Checkbox` primitive. `is_starred` field parity pinned by a `new FormData(form)` assertion; `TasksSection`' toggle carries no form name by design and is pinned by a callback assertion instead. Full history: `docs/ADDENDA_LOG.md` § Design System v2 — pre-auth surfaces.
- **Prospect promotion is not one cross-table transaction, so a lost race can orphan a lead.** ✅ Closed 2026-09-14 — `promoteProspect` now makes one call to the `SECURITY DEFINER` RPC `public.promote_prospect`, which locks the prospect `FOR UPDATE`, re-checks membership for the passed org, and does the `leads` insert, the `lead_submissions` insert and the guarded `where promoted_lead_id is null` claim in one transaction. The orphan-logging path was deleted as unreachable. Two concurrent promotions proven live to leave one lead, one submission, zero orphans. Deferred 2026-08-26. Full history: `docs/ADDENDA_LOG.md` § 2026-09-14 — Prospect promotion becomes one transaction.
- **The `/reports` view cannot be exported.** ✅ Fully closed 2026-09-14. The period filter closed 2026-09-05 — All time / This month / Last month / This year, calendar boundaries in the org's own `organizations.timezone`, closed leads by `closed_at`, open pipeline by `created_at`, no proration. The aggregate export closed 2026-09-14 — `GET /api/reports/export?period=`, one CSV with one header (`section,item,count,amount,rate`) and three sections in page order: pipeline value by status, closed leads by outcome, the win rate. It follows the active period, reuses `escapeCsvValue` from the row-level export, and takes every figure from the same `getPipelineReport` call the page makes. Totals for This month and This year matched the page exactly in the browser. Full history: `docs/ADDENDA_LOG.md` § 2026-08-19 — Reporting view: `/reports`, § 2026-09-05 — Three registered gaps closed: leads CSV export, the `/reports` period filter, one dead declaration, § 2026-09-14 — `/reports` aggregate CSV export.
- **The `/prospects` table has no column resize and no column reorder.** ✅ Closed 2026-09-14 — drag-to-resize on each header's trailing edge (per-column minimum widths, fixed table layout only after the first resize) and drag-to-reorder from a grip in each header, both keyboard-operable, built opt-in in `src/components/ui/table-columns.tsx` over pure state in `src/lib/table/column-layout.ts`; `ProspectRow` places its cells by the header's order. Sorting and `sort.ts` unchanged. Persistence deferred as its own open bullet. Full history: `docs/ADDENDA_LOG.md` § 2026-09-14 — `/prospects` column resize and reorder, session-only.
