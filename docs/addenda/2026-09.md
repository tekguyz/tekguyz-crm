# Addenda — 2026-09

Indexed from [`docs/ADDENDA_LOG.md`](../ADDENDA_LOG.md). Newest last.

---

## 2026-09-04 — The public read-only demo identity

**Why.** tekguyz.com's `/work/tekguyz-crm` case study linked to this app and an
anonymous visitor landed on `/login` — a dead end. The case study claimed a
build existed and then offered no way to see it.

**What shipped.** `https://tekguyz-crm.vercel.app/demo` — one GET, one redirect,
no signup, no password, no interstitial. Behind it is a permanent identity that
holds `SELECT` and nothing else, so it can be published without a reset job, a
cron, rate limiting, or any concern about what a stranger might write.

### The mechanism, and why the obvious one was wrong

The read-only guarantee is a **dedicated Postgres role, `demo_readonly`**, not a
new `VIEWER` value in `organization_members.role`.

The OWNER/ADMIN/MEMBER model lives *above* RLS, inside policy predicates.
Making a `VIEWER` value read-only would have meant a clause added to **9 write
policies** (`leads` ×2, `activity_logs`, `tasks` ×2, `prospects` ×2,
`lead_submissions`, `organization_members`, plus the `storage.objects` upload
policy) **and** a guard added to **9 `SECURITY DEFINER` RPCs**, because a
`SECURITY DEFINER` function runs as its owner and ignores policies entirely:
`import_leads_chunk`, `import_prospects_chunk`, `create_organization_with_owner`,
`accept_organization_invite`, `change_member_role`, `remove_organization_member`,
`vault_set_org_credential`, `vault_clear_org_credential`, `get_org_webhook_secret`.

Eighteen edit sites, each of which must be remembered again for every table and
every RPC added later. **A forgotten clause is a silent write hole.** It fails
open.

Grants are checked *below* RLS. A role holding only `SELECT` is denied every
write on every table with no policy edits, is denied all nine RPCs for free
(no `EXECUTE` grant), and — because Supabase's default privileges grant new
`public` tables to `anon`/`authenticated`/`service_role` and deliberately not to
`demo_readonly` — **a table added next year is unwritable by this role from the
moment it is created.** It fails closed.

**The finding that made it cheap: not one policy in this schema carries a `TO`
clause.** Verified across every migration. They all default to `PUBLIC`, so they
apply to any Postgres role, and `demo_readonly` is covered by the existing read
policies unchanged — still `organization_id in (select private.current_org_ids())`,
still keyed on `auth.uid()`, still resolved through exactly one
`organization_members` row. No second tenant-resolution path was introduced,
which CLAUDE.md's rule 1 forbids.

### How the role reaches a request

PostgREST does `set local role <the JWT's role claim>`. Two candidates could
supply that claim and neither had been run in this project, so the first
implementation step was a throwaway spike rather than a guess:

1. `auth.users.role`, which GoTrue copies into the claim.
2. A Custom Access Token Hook, which needs a Supabase dashboard change.

**Candidate 1 works.** A disposable user's claim moved from `authenticated` to
`demo_readonly` after one `admin.auth.admin.updateUserById(id, { role })` call.
No auth hook is configured and none should be added. `scripts/seed/lib/demo-visitor.ts`
re-asserts both the role and the password on every seed run, so a manual
dashboard edit cannot silently leave the account writable, and rotating the
credential is an env change plus a reseed.

### Fails closed, demonstrated on day one

The first migration granted `SELECT` on eight tables and `EXECUTE` on exactly
one function, `private.current_org_ids()`. The demo could then read every table
it needed and **render no page at all**: `src/app/(app)/layout.tsx` — the app
shell, so every route — calls `public.get_organization_members`, and every page
returned "Something went wrong" with `42501 permission denied for function`.

That is the design working. The first thing that went wrong was the demo being
too locked down, visible on the very first page load. The `VIEWER` failure mode
is a stranger writing to the database, which is visible never.

A second migration (`20260904130000`) grants that one function, narrowly and
after reading its body: it is `stable` so it cannot write, it is self-scoping
(`and p_org_id in (select private.current_org_ids())` in its own `WHERE`), and
it returns no secret. **It is the only function grant this role gets**, and that
was established by probing every read the app performs on a render path in one
pass rather than shipping one migration per bug.

### A real email address was one grant away from being public

`get_organization_members` returns each member's `auth.users` email and the app
shell renders that list on every page. Once `/demo` was public, every member
email in `TEKGUYZ Demo` was on a page strangers could read — and that included
`opsmiller85@gmail.com`, a real address, appearing twice: as a MEMBER and as an
ACCEPTED invite.

`scripts/seed/lib/demo-membership-hygiene.ts` now redacts every non-`@example.com`
membership and invite from the demo org on each seed run. `@example.com` is
reserved by RFC 2606 and can never be a real mailbox, which makes it the one
unambiguous signal that an address in a public demo is synthetic. Auth accounts
themselves are never touched, and the helper refuses to strip an org's last
OWNER.

### The weekly-report leak, fixed as an independent bug

`/api/cron/weekly-report` swept every organization, so `TEKGUYZ Demo` had a
Gemini executive narrative billed and a Resend send posted to
`tekguyz.demo.owner@example.com` — an address that can only bounce — every week.
`create-demo-org.ts` printed a warning about exactly this the day the demo org
was built, and it was never actioned. The sweep now carries
`.eq("is_demo", false)`. Excluded at the query rather than skipped inside the
loop, so nothing downstream runs at all. Verified live: `TEKGUYZ` is swept,
`TEKGUYZ Demo` is not, and no other org's behaviour changes because `is_demo` is
`NOT NULL DEFAULT false`.

### Voice memo: strictly read-only, and one ordering rule

The original brief asked for voice memo capture to "work end-to-end as a
recording" while the account had "zero ability to write anything". Those cannot
both be true — saving a memo inserts an `activity_logs` row and uploads a
storage object. Resolved in favour of strict read-only: an `activity_logs`
carve-out would have broken the guarantee and handed a stranger an unbounded way
to fill the table and the bucket, with no reset job to clean it.

The Gemini skip was added anyway, per the brief, so the guarantee does not
depend on the grant being right. **It sits above `transcribeOrFallback` and that
is load-bearing**: `resolveOrgCredential` falls back to `PLATFORM_GEMINI_API_KEY`,
so a guard placed below it would stop holding the moment that var is set — which
it is, in production. Nothing fails if someone reorders them: green build, every
other test passes. `audio-transcription.test.ts` asserts the ordering at the
source level, the same shape as the `outline-none` guard in
`dropdown-menu.test.tsx`, and was proven to fail before the guard existed.

### Two things found by measuring rather than assuming

**The demo org had no tasks at all.** It seeded leads, `activity_logs` and
prospects, so Agenda, Today and the calendar all rendered empty for a visitor —
which reads as an unfinished feature, not an empty state. `scripts/seed/lib/demo-tasks.ts`
adds eight, spread across overdue, due today and upcoming, plus two completed.
The overdue ones are what exercise the Going Cold SLA styling. No wipe function:
`tasks.lead_id` is `ON DELETE CASCADE`, so `wipeDemoLeads` already removes them.

**Supabase Storage does honour `demo_readonly`.** The design spec flagged as an
open risk that Storage, being a separate service with its own JWT handling,
might assume `authenticated`. It does not — the demo identity lists
`audio-notes` successfully and sees only its own org's folder. The risk did not
materialise and needs no gap entry.

### A standalone `tsc --noEmit` was green only by accident

Found while verifying this wave, and fixed in it: clearing `.next` made `npx tsc --noEmit` start failing with `TS2882` on both side-effect CSS imports (`layout.tsx` and `global-error.tsx`), on an **untouched checkout** — confirmed by stashing every change in this wave and re-running. Next supplies no declaration for a plain stylesheet: `next/types/global.d.ts` covers `*.module.css` and `*.module.scss` only. `next build` passes regardless, because it runs its own type check that already understands the import, so the gap is visible **only** to the standalone `tsc` a session uses to decide the tree is clean. What had been hiding it was a stale artifact from an older Next version sitting in untracked `.next/` — not regenerated by 15.5.20, and gone the moment the directory was cleared. `src/globals.d.ts` now declares `*.css` in one line, so the guarantee no longer depends on build output.

### Verification

`src/lib/demo/demo-visitor.rls.test.ts`, 26 assertions in `npm run test:rls`,
run against the **real** seeded identity and the real `TEKGUYZ Demo` org rather
than a disposable stand-in — a fixture with the same role would not prove that
*this* account is safe. It creates no rows on the happy path, since every write
it attempts is expected to fail, and `afterAll` removes anything that got
through.

One of those tests passed vacuously on its first run: "denies UPDATE on tasks"
returned early because the demo org had no tasks. Replaced with a service-role
fixture task created in `beforeAll` and removed in `afterAll`, so it can never
pass without actually attempting the write.

Through the real UI, the real Server Action and the real database, clicking the
rendered, non-hidden Dismiss control produced:

```
Error: {"code":"42501","message":"permission denied for table tasks"}
```

That is the distinction that mattered: the control is present and clickable, and
Postgres refuses. Nothing is merely hidden.

### Tooling gap closed on the way

CLAUDE.md's standing temp-table dry-run rule was **unenforceable in this repo**:
no `pg` driver, no `psql`, and `supabase db push --dry-run` blocked twice — once
by a CLI account privilege error, then by migration-history drift (remote
carries `20260707215206`, absent locally; not repaired, since that writes to the
history table and is the human's call). `pg` is now a devDependency and
`scripts/dryrun-migration.ts` is the worked example the next migration copies.

Its limitation is stated in the file rather than hidden: `create role` and
`grant` are cluster-level, not session-local, so only the table DDL is genuinely
dry-run.

### Scope notes

Nothing under `src/app/api/v1/triage/` or `src/lib/webhooks/` was touched, no
`leads` DDL, no `LEAD_COLUMNS` change — tekguyz.com's live contact form is
unaffected. `/api/dev-login` keeps its `NODE_ENV === "development"` allowlist
unchanged: it signs in an OWNER, which is a genuinely dangerous identity, and
`/demo`'s reasoning does not transfer to it. No `robots.txt` was created.

---

## 2026-09-04 — The demo read-only badge, and why it is neutral (later same day)

**Why.** `/demo` shipped earlier the same day and the read-only guarantee was
invisible. The org name said "TEKGUYZ Demo", but that names the *tenant* — it
tells a visitor the data is a sample and says nothing about whether they can
save. The first screen alone carries fifteen controls that write, and New Lead
is the most inviting button on it, so the first thing a refusal did was arrive
as a full-page error with nothing having explained why.

**What shipped.** `src/components/shell/DemoModeBadge.tsx`, rendered for the
demo tenant only. Presentation only — the boundary is still the `demo_readonly`
Postgres role, and no grant, policy or RPC was touched.

### Header, not sidebar

`WorkspaceBlock` is `sr-only` when the sidebar is collapsed and absent entirely
on mobile, and a case-study link gets phone traffic. `Header.tsx`'s "exactly two
things" rule is not reopened: the badge renders for `is_demo` tenants only, so
every real tenant's header is byte-identical to before. `getCurrentOrg` already
selected the row, so this is one extra column on an existing embed and adds no
query to a path that runs on every render of every page.

### Three corrections an hour after it shipped

The first version was orange, said `Demo · read-only`, and abbreviated to `Demo`
below `sm`. All three were wrong, and each for a reason this project has already
written down.

**Colour.** Orange put it in the same pill as the SLA `Overdue` badges a few
pixels below it on the Today view — one colour carrying two unrelated meanings
on one screen, and the other one is a real alert. CLAUDE.md's rule is that
colour is signal, not decoration; read-only is a standing condition, not an
alarm. Neutral now.

**Text.** `Demo · read-only` spent half its width on a word the sidebar already
said and the inbound link already promised. `Read-only` is the one fact
available nowhere else, so it became the whole text — which also fits a 375px
header intact and removed the responsive abbreviation, which had been weakening
the message on exactly the devices most likely to open a case-study link.

**Definition.** The neutral pill measures 1.06:1 against the header, so the pill
shape was invisible and the badge read as loose text. A hairline restores it.
Measured live at 8.64:1 text contrast, no horizontal overflow at 375px.

### What is still open

The refusal itself is unchanged: a write Postgres denies still surfaces as the
app's generic error boundary. The badge warns; it does not soften the landing. A
friendlier refusal needs the error boundary to know the session mode, which is a
wider change than a badge. Tracked in `docs/KNOWN_GAPS.md`.

Three tests came with it (`src/components/shell/DemoModeBadge.test.tsx`), proven
to fail with the badge removed.

## 2026-09-05 — Signup is invite-only, and `/login` offers the demo

Public self-serve signup was **live**. `/signup` rendered an open account
creation form to any visitor, `/login` linked to it in as many words ("No
account? Sign up"), and `signUp` in `src/lib/auth/actions.ts` called
`supabase.auth.signUp` with whatever address it was given. Anyone reaching the
production URL could create an auth user; the only thing standing between them
and a tenant of their own was the `/onboarding` form, which
`create_organization_with_owner` would have honoured.

That is attack surface with no legitimate purpose. This product is internal
infrastructure for a services business, not SaaS: there is no billing model, no
ToS, and no review step. The only accounts that will ever exist are provisioned
by hand for one or two trusted collaborators.

### What the audit found, and what it did not

The audit checked every pre-auth-reachable route. `/signup` was the only public
account-creation affordance — `/forgot-password` creates nothing, `/demo` signs
in a powerless identity, and `/invite/[token]` is gated by the token itself.

One thing the docs implied that turned out to be false: `/invite/[token]` does
**not** create an auth user. It renders a preview via `get_invite_preview` and
then sends an unauthenticated visitor to sign in *or sign up*. Signup is not
redundant to the invite flow — it is a **step inside** it. Deleting the route
would have left an invitee with no way to get an account at all.

So the route stays and the gate moves onto it.

### The gate, and why it lives in the action

`src/lib/invites/signup-gate.ts` holds the verdict: a token must name an invite
that exists, is `PENDING`, and has not expired. The predicate itself
(`evaluateInvitePreview`) is pure and clock-injected, so it is tested without a
database.

Both `/signup` and `signUp` run it, and only one of those is the boundary. **A
Server Action is POST-able directly**, so a page that merely hides its form
proves nothing — the action re-runs the check and additionally requires the
submitted address to equal the invited one, which is what stops a leaked token
being spent on an attacker's own account. The page-level check exists so a
stranger sees a sentence instead of a form; the action-level check is what makes
the path closed.

Every refusal returns identical wording, so guessing tokens leaks nothing about
which ones exist. The email-mismatch message is deliberately different — that
caller already holds a live token, so there is nothing left to conceal from
them.

This is routing and presentation only. No policy, grant, RPC or migration was
touched, and none should be added to enforce it.

### Proven by attacking it, not by reading it

The dev server's real `signUp` action id was pulled out of the rendered form and
POSTed to directly, bypassing the page entirely:

- no token, attacker's email → `303 → /login?error=This invite link is not
  valid.`
- valid token, attacker's email → `303 → /signup?...&error=This invite is for a
  different email address.`
- valid token, invited email → reached `auth.signUp`, which answered with
  Supabase's own password-policy error and then its email rate limit.

That last one is the end of the road on this project: email confirmation is on,
and Supabase's built-in SMTP limit is the same blocker `docs/KNOWN_GAPS.md` has
recorded since the original build. It is nonetheless positive evidence — the
error came *from* `auth.signUp`, so the gate let the legitimate request through.
Six unit tests in `src/lib/auth/signup-gate-action.test.ts` assert the same six
outcomes against a mocked client, including that `auth.signUp` is never called
in any refusal.

Supabase also rejects `.invalid` and `example.com` addresses outright, which is
why the live probe ended up on the owner's own `+alias`. The probe invite was
deleted and `npm run check:residue` re-run clean; no auth user was ever created.

### `/login` now has somewhere to send a stranger

The signup link is gone. In its place is **View demo** → `/demo`, the same
`text-accent` inline-link treatment as "Forgot password?" beside it. Measured
live: the colour resolves to each theme's own `--accent` (light
`lab(43.55 14.45 -63.74)`, dark `lab(63.97 5.62 -55.18)`), underlined in both.

Before this, `/demo` had exactly one entry point — a link from tekguyz.com's work
page. Anyone arriving at the production URL from GitHub, a resume or word of
mouth hit a wall.

### Deliberately not done

Landing page, onboarding, OAuth and a "Request access" link were all considered
and left out; the reasoning is recorded in the P1 entry of `docs/ROADMAP.md` rather
than here.

## 2026-09-05 — Three registered gaps closed: leads CSV export, the `/reports` period filter, one dead declaration

Three independently-registered items from `docs/KNOWN_GAPS.md`, done in one pass
because none of them touches the other. No schema, no migration, no RLS policy,
no grant, and no change to `src/lib/webhooks/*` or any lead write path.

### Leads CSV export — `GET /api/leads/export`

A route handler, deliberately not a Server Action: the deliverable is a file, so
`Content-Disposition` can hand the browser a real download with a real filename,
where an action would have to push the whole CSV through the RSC payload and
rebuild it as a Blob on the client.

The column set **is** `LEAD_COLUMNS`, split on commas — `LEAD_EXPORT_COLUMNS` in
`src/lib/leads/export-csv.ts`. That is the whole point of the unit rather than a
detail: that one string already backs eight read sites, and a ninth hand-written
copy is the drift class that produced the field-parity bugs. A column added to
`leads` tomorrow joins the export by itself; a hand-list would have gone stale
silently, and an export missing a column still looks like a working export. A
test asserts the equality directly, so the constant cannot be quietly forked.

Three things are written into the file rather than assumed:

- **The handler authenticates itself.** `updateSession` in
  `src/lib/supabase/middleware.ts` exempts `/api/*` from its login redirect on
  purpose — an API call deserves a status, not an HTML login page — so a route
  under `/api` is **not** gated by the middleware. It calls `getClaims()` and
  returns a 401 of its own. `getCurrentOrg` would have redirected, which is the
  wrong answer for a download.
- **Formula-shaped values are neutralised** with a leading apostrophe on `=`,
  `+`, `-` and `@`, on top of RFC 4180 quoting. `client_name` is
  attacker-supplied through the public webhook, so `=HYPERLINK(...)` in a lead
  name is a real path to a live formula the moment an operator opens the file in
  Excel. CRLF line endings for the same reason — Excel on Windows is the
  realistic consumer.
- **Archived rows are included, and the filters above the button are ignored.**
  An export is an archive, not a view. A backup that silently dropped the rows
  the operator had filtered out would be wrong in the one way an export must not
  be. `archived` is itself a column in the file, so the reader can filter.

Entry point is an **Export CSV** button in the Contacts filter row, built on
`Button`'s `asChild` over a plain `<a download>` — plain, because `next/link`
would try to client-navigate a route that returns CSV.

Live-verified through the real route while signed in via `GET /api/dev-login`:
`200`, `text/csv; charset=utf-8`,
`attachment; filename="tekguyz-demo-leads-2026-09-05.csv"`, `Cache-Control:
no-store`, 20 data rows, header exactly `LEAD_COLUMNS`, and a comma-bearing
address correctly quoted.

### `/reports` period filter — All time / This month / Last month / This year

The three open decisions the gap bullet named were settled rather than
rediscovered, and all three live in `src/lib/reports/periods.ts`:

- **Timezone: the organization's own** (`organizations.timezone`, already
  surfaced as `orgTimezone` by `getCurrentOrg`). Never UTC, never the browser's.
  The aggregation runs on the server, where there is no browser zone to read,
  and "this month" has to mean the same month for every member of a tenant no
  matter where they are sitting.
- **Buckets: closed leads by `closed_at`, open pipeline by `created_at`.** When
  a lead was decided is the only date that makes a period's win rate or realized
  revenue mean anything. An open lead has no `closed_at` at all, so bucketing it
  that way would empty the pipeline figure entirely. `ABANDONED` still buckets by
  `closed_at` and still counts toward period totals; it is excluded from the
  win-rate denominator exactly as before — **the formula itself is untouched**,
  the filter only narrows its inputs.
- **No proration.** A period is a calendar boundary in the org's zone, full
  stop. A part-elapsed month reports the leads it actually has.

The boundary is `[start, end)` — `.gte()`/`.lt()`, never `.lte()` — which is what
keeps a lead landing exactly on a month boundary in one period rather than two.

Two implementation notes worth keeping. The UTC instant of a local midnight is
resolved in **two passes**, because the zone's offset is itself a function of the
instant: the first guess reads the offset in force at UTC midnight, which is the
wrong side of a DST change roughly twice a year. And the wall-clock formatter
uses `hourCycle: "h23"` rather than `hour12: false`, because the latter can emit
hour `"24"` for midnight under some ICU versions and would shift a boundary by a
full day.

Aggregation stays in JS. PostgREST has no `GROUP BY`, and both alternatives are
schema work — the filter is two extra `.gte()`/`.lt()` pairs on the two SELECTs
that already ran.

The bucketing rule was proven by a test that could not even import before this
unit existed (there was no period module at all), and whose boundary pair fails
against any UTC-only reading: in `America/Chicago`, September starts at
`2026-09-01T05:00:00Z`, so a lead closed at `04:59:59Z` is **August**, and one
closed at `05:00:00Z` is September. Verified live too — the seeded demo org reads
16 open / $146,750 / 67% win rate at All time, and 5 open / $33,250 / no decided
leads for This month.

The empty state now distinguishes an empty period from an empty tenant. "No leads
yet" for a quiet month would be a lie about the whole account.

### One dead declaration removed from `globals.css`

`input:not([type="checkbox"], [type="radio"]):focus-visible`'s `border-color:
var(--accent)` was **deleted, not promoted**. It lived in `@layer base`, while
`Input`/`Textarea`/`Select` each carry a `border-hairline` (or `border-danger`)
utility, and utilities sit in a later cascade layer — so the declaration lost on
every field in the app and never painted a pixel. The paired 1px `box-shadow`
accent ring was always the thing carrying the accessibility floor on its own, so
removing the dead line is a no-op visually. A comment in its place says so, and
says where a real focus border would belong instead (a `focus-visible:` utility
on the primitives).

Measured live rather than reasoned about, through both documented traps:
`:focus-visible` did **not** match a programmatic focus until a real key press
had happened in the tab, and the `box-shadow` read back as a transparent `0px`
shadow until transitions were suppressed — the same-task transition-lag artifact
that produced a wrong Known Gaps entry once before. With both handled, a focused
field reads `box-shadow: lab(43.55 14.45 -63.74) 0 0 0 1px` (the theme's
`--accent`) while `border-top-color` stays the hairline `lab(88.39 -0.16 -1.12)`
both before and after focus — which is the proof the deleted line was doing
nothing. The shipped rule now reads, straight out of the CSSOM:
`textarea:focus-visible { box-shadow: 0 0 0 1px var(--accent); outline: none; }`.

### Verification

`npm run build`, `npm run lint` and `npx tsc --noEmit` all exit 0. `npx vitest
run` reports **43 files / 278 tests passing** (from 40 / 250) — 28 new across
three files: `src/lib/reports/periods.test.ts` (12),
`src/lib/leads/export-csv.test.ts` (12) and
`src/lib/leads/report-queries.test.ts` (4), plus the pre-existing suite
unchanged. No row was written to the database by any of it — every new test is
either pure or runs against a mocked client, and the live checks were reads.

---

## 2026-09-05 — Webhook ingestion failure visibility

**Scope: failure visibility only.** No change to signature verification, to
`webhook_secret` rotation, to rate limiting, to replay protection, or to any
response status code for any existing case. No migration, no RLS, no grant.
`src/lib/webhooks/signature.ts` was read for context and not touched.

### The gap

`POST /api/v1/triage/<organization_id>` had no failure path that told anyone
anything. A request that passed signature verification and then failed
downstream — a `leads` insert, the `lead_submissions` write, the activity-log
write — threw out of `ingestWebhookLead` uncaught, so Next returned a bare 500
and that was the entire record of the event. No log line naming the tenant, no
email, nothing to grep. This is the sole intake path for real inbound leads
(the lead-gen CSV import into `prospects` is a separate path and untouched), so
the only feedback loop that existed was a client eventually asking why their
enquiry never arrived. tekguyz.com's client-side zod validation guarantees a
well-formed payload *leaves the browser* and says nothing about what happens
after it lands.

### What shipped

Two new files and three wired ones.

`src/lib/webhooks/ingestion-failure.ts` (new) holds twelve stable error codes,
a `WebhookIngestionError` carrying a code plus a stage, and
`logWebhookFailure`, which prints one line: the prefix
`[webhook-triage-failure]` followed by a single JSON object with `code`,
`organization_id`, `stage` and `detail`. One grep finds every failure; a filter
on `code` or `organization_id` triages it. Vercel already captures
stdout/stderr, so no vendor was added. The module is deliberately not
`server-only` and imports neither a database client nor an email client — same
reasoning as `signature.ts`, and it is what makes it unit-testable in the
hermetic suite.

`src/lib/email/alert-ingestion-failure.ts` (new) sends the operator alert
through the **existing** Resend integration: the same `token_resend`
credential, the same `getOwnerAdminRecipients(orgId, "new_lead")` resolution,
and the same From address — which is now exported from `notify-new-lead.ts`
rather than retyped, so the two cannot drift. No second Resend client
configuration exists. On a successful send it logs the Resend message id (`resend_id=`) alongside the recipient count and the failure code — a same-day follow-up, `f0cc977`, mirroring `sendNewLeadNotification`. Without it, "no error was logged" was the only evidence an alert reached a human, which is not evidence; the early `return` on a send error is what keeps the success line honest. The function never throws: it is called from a failure
path, and an alert that could itself explode would replace one silent failure
with another. Recipients reuse the new-lead list on purpose — the people who
would have received the lead are exactly the people who need to know it never
arrived — and a dedicated opt-out flag was declined because it is a schema
change and this prompt ships none.

`ingest-lead.ts` now throws coded errors instead of `new Error(msg)` at all
four fatal write sites (lead lookup, lead update, lead insert, activity-log
write), plus a fifth via a thin `recordSubmissionOrThrow` adapter —
`recordLeadSubmission` throws a bare `Error` by design because the CSV import
path also calls it, so the webhook code is attached at the one call site that
has webhook context. Its two pre-existing non-fatal `console.error`s (the Spam
Shield alert write, the notification send) now go through `logWebhookFailure`
with their own codes, so they sit in the same greppable channel as the fatal
ones without changing their non-fatal behaviour.

The route catches, reports once, and returns. **The 500 is not new** — an
uncaught throw already produced one; what is new is that a human hears about
it.

### What alerts, and what deliberately does not

A **401 never alerts.** This is a public endpoint and it is scanned
constantly, so a missing, malformed or wrong signature is expected background
noise. Those log at `warn` with `WEBHOOK_AUTH_FAILED` — structured, so the
volume stays measurable — and stop there. Paging a human on scanner traffic
would make the whole channel ignorable within a day, which is precisely the
failure mode this work exists to prevent. A 429 is likewise logged at `warn`
and not alerted: the caller is authenticated, the response tells them what
happened, and a retry recovers it.

Everything **after** a valid signature alerts, including the two 400 cases. A
signature-valid caller is by definition holding the tenant's signing key —
today that is our own contact form — so a body it cannot serialise, or a
payload our schema rejects, is a bug on our side that cost a real enquiry. Same
principle as the classifier-verdict rule: a failure signal routes to a human,
it is never quietly absorbed.

### Never log a secret — what that ruled out

Every line this work prints was written against the rule that a public
endpoint's error path is something that could leak.

- The signing key and the `X-TekGuyz-Signature` value appear in **no** log
  line, no alert body, and not even as a length. The 401 path knows which half
  failed and says only the stage.
- The request body is never logged. On the invalid-JSON path the detail is the
  fixed string `request body is not valid JSON`; on the invalid-payload path it
  is the zod **field names** only (`Object.keys(...fieldErrors)`), because zod
  messages can quote the submitted content — those stay in the HTTP response to
  the signed caller and out of stderr.
- PostgREST errors contribute `message` only. Their `details`/`hint` quote the
  offending column *value*, which is exactly how an enquiry's email address
  would otherwise walk into a log on a unique violation.
- `organization_id` is the only tenant identifier logged. It is not a secret —
  it rides in the URL in plain text — and it is all a triage needs.

### Verification

`npm run build`, `npm run lint` and `npx tsc --noEmit` all exit 0. `npx vitest
run` reports **45 files / 289 tests passing** (from 43 / 278) — 11 new across
two files: `src/app/api/v1/triage/[organization_id]/route.test.ts` (7) and
`src/lib/webhooks/ingest-lead.test.ts` (4).

Each new test was run against the pre-fix tree first (source changes stashed,
test files restored from a scratch copy). **8 of the 11 failed pre-fix**,
including every assertion that carries the new behaviour: the alert-once test,
the no-secret-in-the-log test, both 400-alert tests, all three coded-error
tests, and — perhaps unobviously — the *bad-signature* test, which fails
pre-fix on its `warn` assertion because pre-fix logged nothing at all. The
three that passed pre-fix are honest negative guards and could not have failed:
"no alert on an unknown org id", "no alert on a successful ingestion", and "a
successful ingestion sends exactly one new-lead notification" — pre-fix there
was no alert path anywhere, so their value is as regression guards from here
on, not as proof of a change.

No row was written to the database by any of it: both new files run against a
mocked Supabase client, and no live webhook was fired.


---

## 2026-09-05 — Command palette: prospects as the third source

The palette searched contacts and tasks. `prospects` — the one record type the
operator is most likely to be looking at when they reach for CMD+K, since
`/prospects` is a call list worked top to bottom — was invisible to it. This
adds the third source and closes the Known Gaps bullet outright.

### What shipped

- `searchProspectsForOrg(orgId)` in `src/lib/prospects/queries.ts`. A narrow
  six-column select (`id, name, category, city, phone, status`), **not**
  `PROSPECT_COLUMNS`: the whole array ships to the browser on every palette
  open and the rows only render a name and a subtitle. Same shape and same
  reasoning as `searchTasksForOrg`'s own narrow select.
- `fetchSearchableProspects()` in `src/lib/actions/prospect-actions.ts` — the
  exact sibling of `fetchSearchableContacts` / `fetchSearchableTasks`, resolving
  the tenant with `getCurrentOrg()` so no client-supplied organization id is
  ever trusted.
- A third Fuse index and a third labelled group in `CommandBar`, plus
  `CommandProspectItem`. Render order is index order — contacts, tasks,
  prospects — so arrow keys run continuously across **two** seams now, not one.
- `/prospects?highlight=<id>` as the arrival, and the shared hook below.

### No schema, no policy, no grant

`prospects` was already tenant-scoped by the "Members read tenant prospects"
policy shipped on 2026-08-26. This is a read through the same authenticated
client `getProspects` already uses, so there is nothing new to authorise — no
migration, no `TO` clause, no service-role path, and nothing that reaches for
`execute_sql`. `archived = false` is a product filter, not a security one: the
palette navigates to the default (active) view of `/prospects`, so a hit on an
archived row would land on a page that does not contain it.

### The arrival highlight was extracted, not copied

Prospects have no profile sheet. `/prospects` is a flat table with no
per-record detail view, and inventing a modal or a `/prospects/<id>` route to
give the palette somewhere to land would have been a new surface nobody asked
for. So a prospect result navigates to the table and names the row in the URL.

The mechanism that then marks that row is the same one the Tasks group has had
since 2026-08-28. It now lives once, in
`src/lib/hooks/use-arrival-highlight.ts`, and both `TasksSection` and
`ProspectsTable` consume it. A second copy would have been the same drift class
as a hand-copied primitive — CLAUDE.md's standing rule.

The hook's `onArrive` callback is the part that is easy to miss, and it is why
this is one mechanism rather than two coincidentally similar ones. **The row
must be in the DOM before it can be scrolled to.** `TasksSection` uses it to
switch its Open/Completed tab, because a completed target is not rendered at
all under the default Open tab. `ProspectsTable` uses it to clear its search
box and status filter, because both are client state that survives a
client-side navigation — an operator who has narrowed to CALLED and then picks
a NEW prospect in the palette would otherwise arrive at a page that does not
show it, and the feature would silently do nothing. Same failure, same fix,
same code.

The marker itself is `TaskRow`'s idiom reused, not new visual language: an
`--accent` bar down the leading edge over a `--canvas-soft` tint. On a `<tr>`
it is drawn as an inset `box-shadow` on the first cell rather than a border or
a pseudo-element — a `<tr>` is a poor containing block, and a real border would
shift every cell in the row by its own width.

### Explicitly out of scope

The pagination/debounce gap. It is the same component and the bullet says the
two are worth revisiting together, but the largest source is 122 prospects and
each group is already capped at 8 rendered rows by `MAX_RESULTS`. Pulling it in
as a side effect of this prompt would have been scope creep; the bullet stays
open in `docs/KNOWN_GAPS.md` with its scale note refreshed to say there are now
three unpaginated fetches, not two.

`fetchSearchableContacts` and `fetchSearchableTasks` are **unchanged** — no
edit of any kind — and so are the RLS policies, every migration file, and
`ProspectsTable`'s own sort/filter code beyond the one `onArrive` reset.

### Verification

`npm run build`, `npm run lint` and `npx tsc --noEmit` all exit 0. `npx vitest
run` reports **46 files / 298 tests passing** (from 45 / 289) — 9 new: 4 added
to `src/components/command/CommandBar.test.tsx` (from 8 to 12) and a new
5-test `src/components/prospects/ProspectsTable.test.tsx`.

The arrival tests were run against the pre-fix shape twice, not merely against
the fix:

- With `highlighted` unwired from `ProspectRow`, **4 of the 5** ProspectsTable
  tests fail. The fifth is the honest negative guard ("highlights nothing when
  no prospect id is passed") and could not have failed.
- With the `onArrive` callback removed but everything else intact, exactly
  **1** fails — "clears a filter that excludes the target before marking it",
  which is the whole point of that callback existing.

Fetch-once-per-open is asserted for all three sources in one test, by typing
into the combobox after the palette is open and pinning each mock at exactly
one call. Cross-tenant is asserted the same way the other two sources are: the
source is RLS-scoped server-side, and the palette is pinned to render exactly
the rows that boundary returned and to invent none.

**Browser verification was not reached, and that is being stated rather than
implied.** The dev server is up and the signed-in `/prospects` route returns
200 through the real `/api/dev-login` path, with `?highlight=<uuid>` accepted
and rendering the page. But the Browser pane came up with
`document.visibilityState === "hidden"` — the environmental state CLAUDE.md
§ Session & Verification Discipline documents — and the check it prescribes
confirmed it: the **already-shipped** command palette also refused to open,
both from a scripted click on its trigger and from a dispatched CMD+K. So the
fallback applies and the client-side behaviour rests on the unit tests above,
which is what they were written to carry.

No row was written to the database by any of it.

---

## 2026-09-07 — Shell/IA Stage 1, prompt 1 of 4: the sidebar, header and mobile tab bar

Design exploration only. **Nothing under `src/components/shell/` changed** — the
shipped sidebar, header and tab bar are byte-for-byte what they were, and
`git status` after the work showed new files only. Three named variants live at
dev-only routes and the owner picked one the same day.

### The routes

`/shell` is an index; `/shell/sectioned`, `/shell/disclosure` and `/shell/dock`
are the variants. All four are outside the `(app)` route group, the same as
`/design`, because each renders its OWN shell chrome as a comp — inheriting the
real `AppShell` would put two sidebars and two headers on one screen.

They are gated three ways, and the third is the one that matters:

1. `updateSession` names no exemption for `/shell`, so an unauthenticated
   production request is redirected. Measured: `307 → /login` on all four.
2. `src/app/(dev)/shell/layout.tsx` refuses the segment on any build whose
   `NODE_ENV` is not `development` — an allowlist, matching `/api/dev-login`
   rather than `/design`'s `!== "production"` denylist.
3. **Every `page.tsx` repeats the check, and that repetition is the actual
   gate.** A layout that throws does not stop its own page rendering: React
   renders the two concurrently. With the check only in the layout, a
   production build still prerendered **39,170 bytes of real comp markup** into
   `.next/server/app/shell/sectioned.html` and served it inside the 404
   response. With the check at the top of each page, that file is 16,469 bytes
   of Next's own 404 and carries none of the comp's own strings. This is now a
   permanent rule in `CLAUDE.md` § Build discipline.

Verified on `NODE_ENV=production npm run build && npm start`, signed in through
the public `/demo` session so no credential was typed: every `/shell` route
returns a payload carrying `NEXT_HTTP_ERROR_FALLBACK;404` with
`own-markup=False`. `/design` measures identically, which is what confirmed it
was never broken. The HTTP status stays **200** for all of them because the
root `loading.tsx` commits the response before any page body runs;
`/api/dev-login` returns a true `404` only because a Route Handler has no such
boundary.

### The three variants, and what each was testing

Same seven destinations in all three, grouped identically so the screenshots
compare one variable: **Work** (Today, Pipeline, Contacts), **Prospecting**
(Prospects, Import), and a footer pair (Reports, Settings). `nav-groups.ts`
looks every entry up out of `PRIMARY_NAV`/`SECONDARY_NAV` by href and throws on
a miss, so a comp cannot invent a route.

- **A — Sectioned.** Static uppercase captions, no disclosure. Collapsed, the
  captions become hairline rules and stay in the DOM as `sr-only` so each group
  keeps an accessible name. Header 48px with a fixed 256px field-shaped search
  trigger. Active phone tab gains an `--accent` plate at 12%.
- **B — Disclosure.** The Plan CRM reference adapted: collapsible groups, the
  one holding the current route open, the rest closed. Parents are `<button>`s,
  never links — there is no route behind "Work" and inventing one was out of
  scope — and the open parent is deliberately NOT accent-highlighted, because
  `--accent` on a nav row means "you are here" and a parent is never anywhere.
  Collapsed, the disclosure is dropped rather than shrunk: a 56px rail has no
  room for a chevron, an indent guide or a caption.
- **C — Quiet (picked).** No captions at all; a full-bleed hairline between
  groups and position doing the work. Header carries the page title on the
  left and Help / search / seam / avatar on the right. Phone bar lifts into an
  inset rounded card.

### Two review rounds, and what the second one changed

The owner reviewed the first screenshots against the reference and raised the
collapse chevron and a busy sidebar footer. Both were real.

**The collapse control moved from a footer strip to the panel's top-right
edge**, in all three variants. That removed a whole band from the bottom of
every rail. It sits *inside* the edge rather than straddling it the way the
reference draws it, because the shipped `<aside>` is `overflow-hidden` —
load-bearing for the two-layer translate collapse — so anything overhanging
would be clipped. Collapsed, the chevron cannot share the workspace row (the
mark is centred and 56px has no second slot), so it becomes the first row of
the rail: still one control, still no extra band. The icon is a directional
chevron rather than the shipped `IconLayoutSidebarLeft*`, because at the
panel's edge the position already says which panel it is about.

**Variant C's original thesis was deleted by its own comp.** It had docked
identity — avatar, name, email, chevron — in the sidebar footer. On screen that
band plus the nav footer plus the New-lead CTA plus the collapse strip stacked
four deep and made the bottom of the rail the busiest region on the page.
Identity went back to the header's top-right, which is where the reference puts
it, where the shipped shell already has it, and — unlike the sidebar — a place
that still exists on a phone. The variant kept its route name (`/shell/dock`)
and lost its display name, which is now "Quiet".

### The binding decision

**Variant C, picked 2026-09-07.** With it, two placement rules:

- **Help is a header icon button**, to the left of the avatar. It is the one
  control a first-week user reaches for and a menu hides it behind a click.
- **Settings stays a sidebar destination and is NOT moved into the avatar
  menu.** It is a real page with org details, members, webhook rotation and API
  keys behind it; burying a destination in an account popover makes it
  unfindable and un-linkable. The avatar menu keeps only account-scoped items:
  who you are signed in as, theme, sign out.

This contradicts `docs/DESIGN.md` § The Application Shell decision 2 ("the
header holds exactly two things") and decision 3 ("sidebar navigation is
flat — permanently"). Both are re-opened deliberately by this pick, and neither
`DESIGN.md` nor the shipped shell is edited until Stage 2 wires it in.

### The one new primitive — built twice, kept once

**Superseded 2026-09-09. This prompt's `Avatar` no longer exists.** It was
written on one machine while a separate Avatar-primitive prompt built its own
on another, on the same day and neither aware of the other; both were correct
work against the same brief and both reached the same conclusions
independently. The other one committed and pushed first (`2a997b4`), had
already been registered on `/design`, and is therefore the one that survives.
This prompt's `src/components/ui/Avatar.tsx` and `src/lib/ui/avatar-identity.ts`
were **deleted, not merged** — a merge would have produced a third API — and
the three variant headers were rewired to the surviving primitive's single
`name` prop. `docs/KNOWN_GAPS.md`'s test count reflects the survivor, not the
319/48 this section originally claimed.

What the two agreed on without conferring is the part worth keeping: no avatar
primitive existed beforehand (the only repo matches for `avatar|initials` were
29 instances of `initialState`); FNV-1a over a normalised key; the six
decorative pill hues; `AVATAR_TONES` order as contract; no Radix. They differ
only in the seed (this one hashed the email, the survivor hashes the name) and
in the prop shape. The reasoning below is preserved as the record of how this
one was argued, not as a description of code in the tree.

`src/components/ui/Avatar.tsx` — initials, a colour picked by hashing the
email, zero storage.

The live shadcn registry was queried, not recalled: `@shadcn/avatar` exists and
is entirely Radix `Avatar.Root`/`.Image`/`.Fallback`, whose whole job is
tracking an `<img>` load state so a fallback can appear after it fails. This
app stores no avatar images, has no column for one, and does not depend on
`radix-ui`. So the shape was kept — `data-slot="avatar"`, the
`sm`/`default`/`lg` size names, `rounded-full`, `shrink-0`, `select-none` — and
the Radix layer was dropped, with the reason written into the file and a note
that real photo upload is the thing that would earn the dependency back.

Colour comes from the six decorative pill hues. `--accent` has four sanctioned
uses and an avatar is none of them, so it was not available; the pill palette
is the only other sanctioned source, its pairs are already contrast-audited in
both themes, and a person's avatar is the same kind of signal a category dot
is. It is never used here as a border, a button or a surface. **Flagged to the
owner as a stretch of the "badges and dots only" wording, and accepted.**

The seed is the **email, lowercased — never the display name**, which is
editable through `updateDisplayName` and would silently re-colour someone the
moment they fixed a typo. The order of `AVATAR_TONES` is part of the contract:
reordering it re-colours everyone.

This pulls the **Avatar images** item in `docs/ROADMAP.md` into partial scope —
the initials half now exists as a primitive. It has **no production consumer**;
only the dev comps import it. *(That ROADMAP bullet was removed outright by the
surviving prompt in `2a997b4`, which is the state on `main`. Photo upload
itself remains unstarted and is now tracked in the CLAUDE.md § 3 Avatar row.)*

### Two silent failures found while building

**A `"use client"` module's plain constant read as nothing in a Server
Component.** `PHONE_TAB_BAR_OVERRIDE` — the string that repositions a comp's
tab bar inside the phone frame — lived in `PreviewSurface.tsx`. Next replaces a
client module with client-reference proxies, so the Server Component page got a
reference rather than the string: the rendered `nav`'s class list simply ended
at `fixed md:hidden` with the override absent, and the phone bar pinned itself
across the bottom of the whole page. `tsc`, `eslint` and the build were all
clean. Moved to a plain sibling module, `preview/frames.ts`. This was already a
`CLAUDE.md` rule as of the same day; this is the second instance of it.

**The prerender hole above**, which is the more expensive one and is now its own
`CLAUDE.md` bullet.

### Checks

`npm test` 317/48 as this prompt originally ran — 19 of those were its own
`src/lib/ui/avatar-identity.test.ts` (13) and `src/components/ui/Avatar.test.tsx`
(6), both deleted with the duplicate primitive above. **Re-measured 2026-09-09
after the rewire: 316/47**, which is the surviving Avatar's own mark and the
figure `docs/KNOWN_GAPS.md` carries. `npx tsc --noEmit`,
`npm run lint` and `npm run build` all clean. `npx impeccable detect` over all
23 touched files: zero findings.

Browser verification on the real dev server, both themes: the focus ring on a
nav row measures `2px solid` `--accent` at `2px` offset with `:focus-visible`
matching, confirmed by screenshot as well as by `getComputedStyle` — no
`outline-none` was copied onto any new interactive row. Variant B's disclosure
was driven with a real pointer and cross-checked at the DOM: `aria-expanded`
flipped and the closed panel's `[hidden]` resolved to `display: none`, so its
three links are out of the tab order rather than merely invisible.

No row was written to the database by any of it.
## 2026-09-09 — Doc checks on a PostToolUse hook, and the browser 16 tests never used

Two pieces of work, and the second was found by doing the first.

### The hook

The four check scripts in `scripts/` all worked and nothing ran them. They now
run automatically, from a `PostToolUse` hook in `.claude/settings.local.json`
matching `Edit|Write|NotebookEdit`:

```
node "$CLAUDE_PROJECT_DIR/scripts/check-design-drift.mjs"; node "$CLAUDE_PROJECT_DIR/scripts/check-section-pointers.mjs"; exit 0
```

**The trailing `exit 0` is the whole design.** A PostToolUse hook that exits
non-zero blocks the tool call. A drift finding is information, not a veto — the
edit that surfaced it is usually the edit that was supposed to happen. Both
scripts still exit 1 on findings and 2 on "could not complete"; the hook reports
those and swallows the status.

Proven by breaking something on purpose rather than by reading the config:
`docs/DESIGN.md`'s `rounded-md` row was changed from `6px` to `99px`, an edit
was made, and the hook printed

```
design-drift: 1 MISMATCH(ES) — scanned 12 colour tokens (x2 themes), 8 type roles, 5 radius steps.
  radius  --radius-md: DESIGN.md states `99px`, globals.css has `6px`.
```

while the edit itself still succeeded. The value was then restored and the run
went clean. A hook nobody has watched fire is a hook nobody has tested.

**Two of the four scripts are deliberately not on the hook, for two different
reasons.**

`check-test-residue.mjs` is excluded because **an edit cannot create residue.**
Residue appears when a seed script runs, a webhook is POSTed, or a form is
submitted — none of which are file edits. Hooking it would ask the question
thousands of times when the answer could not have changed, and stay silent at
the one moment it mattered. This is a wrong-trigger argument, not a cost one:
the script needs only `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SECRET_KEY`,
both present in `.env`, and it completes in 5.8s. An earlier belief that it was
"too expensive" was checked against a real run and is wrong.

`check-doc-figures.mjs` is excluded because it runs the **entire vitest suite**
(`scripts/check-doc-figures.mjs:59`) to verify the test-count mark. That was
83s. Which is what led to the rest of this entry.

### The browser 16 tests never used

`vitest.config.mts` set `environment: "jsdom"` once, for everything. That is the
React-starter default and it was wrong here: **16 of 47 test files never touch
the DOM** — phone formatting, CSV export, HMAC signatures, report periods — and
each was paying a full fake-browser boot to do arithmetic.

Measured on the 8 heaviest of those pure-logic files:

| | environment setup | actual tests |
|---|---|---|
| `jsdom` (as configured) | **52.37s** | 0.589s |
| `--environment=node` | **0.006s** | 0.792s |

Across the whole suite the figure was `Duration 81.38s ... environment 408.94s`
— the fake browser cost 3.4x the tests it existed to support.

The fix is a two-project config: `dom` (jsdom, `setupFiles`, the react plugin,
`src/**/*.test.tsx` plus the one DOM test that is a `.ts` file) and `node`
(no environment, no setup file, no plugin, everything else). `setup.ts` imports
`@testing-library/jest-dom` and `@testing-library/react` at the top level, so
scoping it to the `dom` project is what actually removes its cost rather than
just relocating it. After: same **47 files, 316 tests**, `environment` down to
~247s, `check-doc-figures.mjs` down to 62.6s.

**Why this went unfixed for so long — three traps, all of which teach the wrong
lesson.**

1. The old config comment blamed "machine speed" and raised `testTimeout` to
   10s. That hides the symptom, so nobody measures again.
2. The obvious remedy makes it worse. Capping the suite at 4 workers measured
   **118s against 81s** — the constraint is memory per jsdom worker, not cores
   (7.7GB total, ~1.8GB free, 12 logical cores). "Reduce parallelism" failing is
   what makes the whole thing feel unfixable.
3. It surfaced as *timeouts*, not slowness. The suite sat ~30s under the 120s
   ceiling of whatever invoked it, so a busy machine tipped it over and it read
   as flakiness in the tests themselves.

**`pool: "threads"` was tried and rejected, and the reasoning is the durable
part.** It measured 54.90s against forks' 68.78s — until the *same fork config*
later measured 51.98s, beating the threads run it had supposedly lost to.
Wall-clock on this machine swings ~20s with free memory, so **no single-run A/B
here proves anything**; take a median across runs or do not claim a win. What
was not noise: across 7 threads runs, one failed 3 tests in 2 files, and 5
further runs could not reproduce it. Threads share module state that forks
isolate. An unexplained 1-in-7 failure, bought with a speedup that may not
exist, is the exact thing this change was made to stop.

No schema, no migration, no RLS, no grant, and no test was added, removed or
skipped. `npm test` 316/47, `npx tsc --noEmit` clean, `eslint` clean. The hook
file itself is gitignored, so it is local to this machine and travels in this
entry only.

### Later the same day — the count is cached, so a handoff no longer runs the suite

The 62.6s above was still the wrong shape, and the reason is who pays it. The
`handoff` skill runs `npm run check:docs` **every time** (`.claude/skills/handoff/SKILL.md:54`),
and check:docs runs `check-doc-figures.mjs`, which runs all 316 tests to
re-derive one number. Ten handoffs in a day is ten full suite runs — roughly ten
minutes spent re-deriving a figure that changes maybe twice a week.

The count is a pure function of the test files plus the vitest config, so it is
now cached against a SHA-256 of exactly those inputs — every non-`.rls`
`*.test.ts`/`*.test.tsx` under `src/`, plus `vitest.config.mts` and
`src/test/setup.ts` (both decide what counts as a test and how files group into
suites). Cache lives at `node_modules/.cache/tekguyz-doc-figures.json`, already
gitignored and wiped by a reinstall, which is the right failure mode: it
re-derives. `DOC_FIGURES_NO_CACHE=1` forces a real run.

**The guarantee is unchanged, and that was proven by attacking it rather than by
reading the diff.** Four runs:

| | result | time |
|---|---|---|
| Cold, no cache | OK 316/47 | 62.3s |
| Nothing changed | OK 316/47 `(cached)` | **0.5s** |
| One test appended to `periods.test.ts` | **drift: claims 316, reports 317** | 62.5s |
| `KNOWN_GAPS.md` edited 316 → 315, cache untouched | **drift: claims 315, cached says 316** | 0.6s |
| Cached fingerprint corrupted by hand | OK 316/47, refused the cache | 70.6s |

The third and fifth rows are the ones that matter: the only edit that can change
the count is also the only edit that busts the hash, so a stale count stays
impossible. The fourth is the one that answers "is 0.5s doing anything?" — the
fast path still compares against the doc and still fails, it just does not
re-derive an input that cannot have moved.

`npm run check:docs` end to end is now **3.8s** warm, against ~65s before.
`check-doc-figures.mjs` stays off the PostToolUse hook regardless: a cold run
after any test edit is still ~60s, which is not a per-keystroke cost.
