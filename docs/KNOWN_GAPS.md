# TEKGUYZ CRM — Known Gaps

Relocated from CLAUDE.md on 2026-08-14. It is a register of deliberately
deferred work, not an instruction set, and it was the documented cause of both
CLAUDE.md compression passes.

## How to maintain this file

**This section is the single copy of the policy.** CLAUDE.md used to restate it
in two places; both are now one-line pointers here. Keep it that way.

- Anything intentionally deferred gets a bullet here — a limitation, a missing
  enforcement, or a scoped-out edge case accepted on purpose rather than
  forgotten — so it is never silently assumed complete in a later session. That
  includes a deliberate v1 non-goal of a shipped initiative: CLAUDE.md § 3
  records status only, never scope that was left out.
- Open items are ⬜, one to two sentences, with a date and a pointer to the full
  story in `docs/ADDENDA_LOG.md`.
- The moment an item is fully resolved, move its line to
  `docs/ADDENDA_LOG.md` § "Known Gaps — Resolved Items Archive" in that same
  session. Do not leave resolved items here "to clean up later" — deferred
  cleanup is exactly what let this register regrow to the point that CLAUDE.md
  needed two separate compression passes. Relocation is part of closing the
  gap, not a follow-up task.
- A ✅ shown beside a ⬜ means partially resolved with real open scope — the ✅
  half is context for why the ⬜ half is scoped as it is.
- An item with no date is stale. Re-triage it before relying on it.
- Full discovery narrative and historical context for every item — including the
  complete pre-2026-07-26 text of this register — lives in
  `docs/ADDENDA_LOG.md` § "Known Gaps — Full Historical Record", plus whichever
  addendum each individual bullet names.
- An item that was considered and **rejected outright** is not deferred work and
  does not belong under Open items. It goes in the subsection directly below,
  which overrides any older history that still reads as open.

### Permanently rejected — never re-list

A third disposition, distinct from both of the above. An **Open item** is work
deferred on purpose. A **Resolved** item is work that got done and moved to
`docs/ADDENDA_LOG.md` § "Known Gaps — Resolved Items Archive". An item here is
neither: it was considered and **rejected**, permanently. It is not deferred,
not partial, and not waiting on a trigger.

Dated mentions in `docs/ADDENDA_LOG.md` are left exactly as they are — they are
a correct record of what was true on their own date, and they are not the bug.
This list is what overrides them going forward. **When history reads as open and
this list says rejected, this list wins.**

- **Leaked Password Protection** — rejected 2026-07-22, paid Supabase tier only,
  this project stays on free tier indefinitely. Never list as open work or a
  "needs user" action in any handoff, regardless of what `docs/ADDENDA_LOG.md`
  history says. Its advisor lint (`auth_leaked_password_protection`, WARN) will
  keep firing on every live `get_advisors` run — that is expected, and it is not
  a finding. Its Open-items bullet was deleted on 2026-08-15; this entry replaces
  it so the deletion cannot be undone by a later reader of the history. Full
  history: `docs/ADDENDA_LOG.md` § Known Gaps — Full Historical Record
  (2026-07-22 triage) and the 2026-08-15 hygiene pass.

- **Every other Supabase Security Advisor lint this project reports** — rejected
  2026-08-18 after auditing all 14 against the live schema, not from the doc.
  **Expected output, not findings, and they will keep firing forever.** Two INFO
  `rls_enabled_no_policy` (`organization_credentials`, `report_sends`) are the
  *safest* possible state — RLS on with zero policies denies everything, and
  neither table grants anything to `anon`/`authenticated`; adding a policy to
  quiet the lint would make them less safe. Ten WARN
  `authenticated_security_definer_function_executable` describe this project's
  entire multi-tenant write model, which is deliberately built from
  `SECURITY DEFINER` RPCs; all ten were confirmed to re-check the caller inside
  their own bodies. One WARN `anon_security_definer_function_executable` on
  `get_invite_preview` is required — `/invite/[token]` renders before sign-in.
  Do not "fix", suppress, or re-investigate any of them; do not list them in a
  handoff. The only one that could ever change is leaked-password protection
  above, and only on a paid tier. Full audit: `docs/ADDENDA_LOG.md`
  § 2026-08-18 — Security Advisor: all 14 lints triaged, none actionable.

## Open items

- **Task/Calendar v1 shipped with four deliberate non-goals.** ✅ Task edit and removal closed 2026-08-19 — a task's title, description and due date are now editable, and `tasks.dismissed` removes a task from every active surface without deleting the row (no DELETE grant and no DELETE policy were added; `authenticated` still holds neither). ⬜ Still open by design: no calendar view; unarchiving a lead does **not** reopen the tasks that were auto-closed when it was archived; and no task `assigned_to` — `leads.assigned_to` shipped 2026-08-18, `tasks` deliberately did not, so task assignment is unblocked but simply unbuilt. Full history: `docs/ADDENDA_LOG.md` § Task/Calendar addendum — Prompts 1 & 2, § Prompt 4, § Prompt 5, § 2026-08-19 — Task editing and non-destructive dismiss.
- **`/signup`'s `minLength={6}` is weaker than Supabase's real password policy.** ⬜ Registered 2026-09-05, found while live-testing the new invite gate: the browser lets a six-character password through, then Supabase rejects it server-side with its own full character-class message, so the user learns the rule only after submitting. Cosmetic, not a security hole — the server rule is the one that binds — and deliberately out of that unit's fence. Fixing it properly means reading the real policy rather than guessing a second copy of it into the form. Full history: `docs/ADDENDA_LOG.md` § 2026-09-05 — Signup is invite-only, and `/login` offers the demo.
- **The `/reports` view cannot be exported.** ✅ The period filter closed 2026-09-05 — All time / This month / Last month / This year, resolved as calendar boundaries in the organization's own `organizations.timezone`, with closed leads bucketed by `closed_at`, open pipeline by `created_at`, and no proration of a part-elapsed period. ⬜ Still open by design: exporting the aggregate. It shares no machinery with the row-level leads CSV export that shipped the same day — that serialises `LEAD_COLUMNS`, whereas this would have to serialise a computed shape (stage rows, outcome rows, a win rate) that has no table behind it, and nobody has yet said which of those a spreadsheet should contain. Full history: `docs/ADDENDA_LOG.md` § 2026-08-19 — Reporting view: `/reports`, § 2026-09-05 — Three registered gaps closed: leads CSV export, the `/reports` period filter, one dead declaration.
- **Signup-confirmation path, post `/auth/confirm` PKCE fix.** ⬜ Consciously deferred — the fix should carry over by construction, but is not live-email-verified due to Supabase's auth rate limit. **Re-measured 2026-09-05 and still blocked** — a real invited signup through the new invite gate reached `auth.signUp` and came back `email rate limit exceeded`, so the limit has not cleared in the intervening five weeks and custom SMTP remains the only way past it. Full history: `docs/ADDENDA_LOG.md` § Signup-confirmation live-email re-check attempt, § 2026-09-05 — Signup is invite-only, and `/login` offers the demo.
- **No account-level (user-scoped) settings surface exists.** ✅ Mostly fixed 2026-07-27 — `AccountPanel` on `/settings` covers password change, display name, and notification preferences. ⬜ Still open by deliberate scope: email change, account deletion (needs its own decision about an OWNER's leads first), and an org switcher (no persisted "active org" concept exists yet). Full history: `docs/ADDENDA_LOG.md` § Account Panel: Password, Display Name, Notification Preferences addendum.
- **3 of 5 `organization_credentials` vault columns have no UI.** ⬜ Partly stale as of 2026-08-11: OpenAI and Twilio genuinely have no caller, but **Resend now has two** (`notify-new-lead.ts`, `send-weekly-report.ts`) — they resolve it via the `PLATFORM_RESEND_API_KEY` env fallback, so per-org BYO Resend is the only piece actually missing. Full history: `docs/ADDENDA_LOG.md` § Settings & Configuration Inventory.
- **The webhook signature carries no timestamp or nonce, so a captured request can be replayed.** ⬜ Registered 2026-08-18 when HMAC signing shipped — the signature covers the body only, so an attacker who captured a valid signed request over a compromised transport could resend those exact bytes and have them accepted. Deliberately not built now: the blast radius is a re-submitted enquiry that already arrived, and repeat enquiries collapse onto the same lead rather than creating duplicates, so a replay adds a submission row rather than corrupting anything. The fix when it is warranted is a timestamped signature (sign `timestamp.body`, send the timestamp in its own header, reject a skew beyond a few minutes) — a breaking protocol change for every caller, which is why it is not being bundled in. Full history: `docs/ADDENDA_LOG.md` § 2026-08-18 — HMAC request signing replaces the URL-path webhook secret.
- **AI Spam Shield over-triggers on the tekguyz.com form's own placeholder copy.** ⬜ Reported not fixed (2026-08-11) — deliberately out of scope of the routing fix, since tuning a classifier is its own change with its own verification. The blast radius is now capped (flagged leads stay visible and still notify), so this is a precision problem, not a data-loss one.
- **`getCurrentOrg()` multi-membership resolution.** ✅ Resolution fixed 2026-08-17 by `94de14b` — it now orders by `organization_members.created_at ASC` (oldest membership wins, stable across sessions) and `console.warn`s on an exact `count > 1` rather than silently dropping the other memberships, which is exactly the decided fix. ⬜ Still open: that `count > 1` branch has **never been exercised against real data** — no user in the live database holds more than one membership, so it is code-inspected only. The org switcher stays separately deferred (bullet below). Full history: `docs/ADDENDA_LOG.md` § 2026-08-16 — Wave decisions, § 2026-08-17 — Deterministic org resolution, and hiding lifecycle controls from a MEMBER.
- **`lead_source`/`service_category` are free text with no managed vocabulary.** ⬜ Deferred as P3 (2026-07-30) — a select/typeahead would help analytics grouping, but needs its own decision about the option list and migrating existing free-text values. Revisit when analytics grouping actually matters.
- **A lead can still name a former member if the membership row was deleted outside `remove_organization_member`.** ⬜ Narrow residual (2026-08-18) — the supported removal path now releases assignments in the same transaction, so this is no longer reachable through the app. It remains reachable by a direct service-role `DELETE` on `organization_members` (a script, or Supabase Studio), because the cleanup lives in the RPC rather than in an FK or a trigger — `leads.assigned_to` references `auth.users(id)`, and no constraint can observe a membership row disappearing. Deliberately not silent when it happens: `AssignmentField` renders the stale value as a "Former member" option, `AssigneeLabel` shows "Former member" on the card, and the assignment trigger's `IS DISTINCT FROM` fast path keeps unrelated edits to such a lead legal. A `BEFORE DELETE` trigger on `organization_members` would close it completely; not built because nothing in the product deletes membership rows any other way. Full history: `docs/ADDENDA_LOG.md` § 2026-08-18 — Team management: role change and member removal.
- **A MEMBER still sees every lead in the tenant; `assigned_to` is ownership, not visibility.** ⬜ Open, unscoped by decision (2026-08-18) — per-row ownership now exists and is filterable ("My leads"), but no RLS policy reads it and none was added. Narrowing SELECT by assignee is a product decision nobody has made, and it would be the first time this schema hid tenant data from a tenant member. Do not treat the column's existence as implying it. Full history: `docs/ADDENDA_LOG.md` § 2026-08-18 — `leads.assigned_to`: per-lead ownership.
- **Command palette (Prompt 8) has no pagination or debounce.** ⬜ Consciously deferred; scale note refreshed **2026-09-05**, when the Prospects source was added — the palette now fetches **three** sources per open (`fetchSearchableContacts`, `fetchSearchableTasks`, `fetchSearchableProspects`), all of them whole-tenant and unpaginated, and ranks them in the browser with three Fuse indexes. Deliberately **not** pulled into that prompt's scope, and it did not need to be: the largest source is prospects at **122 rows in TEKGUYZ** (7 synthetic in `TEKGUYZ Demo`), against low tens of leads and tasks — still trivially fast, and each group is already capped at 8 rendered rows by `MAX_RESULTS`. What changed is only that there are now three unpaginated fetches instead of two, which moves the revisit trigger nearer without reaching it. Revisit when any one source passes low hundreds — prospects is the one that will get there first, since it grows by CSV import rather than by hand. **1 lead in the real TEKGUYZ org, 20 in TEKGUYZ Demo**, counted live via read-only SQL. Still trivially fast. Earlier marks: 14/20 on 2026-08-11, which was already stale before it was next read — the sixteen test leads named in CLAUDE.md § Test-Data Cleanup were removed after it was written; and a "0-lead" justification before that. Revisit around low hundreds of contacts. Full history: `docs/ADDENDA_LOG.md` § Known Gaps — Full Historical Record.
- **No org switcher — the slot is reserved, the switcher is not built.** ⬜ Open (2026-08-16) — the shell redesign built `WorkspaceBlock` to render the mark and org name as static content, shaped so it can become a `DropdownMenuTrigger` later without restructuring the sidebar. The prerequisite is a **persisted "active org" concept**, which does not exist: `organization_members` can hold several rows for one user and nothing records which one is current, so switching needs a migration plus resolution plumbing in `getCurrentOrg()`, not a menu. (The resolution half landed 2026-08-17 in `94de14b` — `getCurrentOrg()` is now deterministic and reports extra memberships. The persisted “active org” migration, and therefore the switcher, is still not built.) Deliberately out of the shell prompt's fence, which had no database surface at all. Overlaps the account-settings bullet above, which names the same missing concept. Full history: `docs/ADDENDA_LOG.md` § Application shell redesign.
- **`get_advisors` (security + performance) findings.** ⬜ Re-triaged 2026-08-14 — still mostly by-design or non-issues at current scale (unindexed FKs, `auth_rls_initplan` warnings, unused indexes, RLS-enabled-no-policy on service-role-only tables). New since the 2026-07-22 pass: nine SECURITY DEFINER warnings from the newer 0028/0029 lints. Those are **new lint rules, not new exposure** — all nine are intentional RPC endpoints, and all nine were checked for an internal caller/role gate: eight on 2026-08-14 (see `docs/ADDENDA_LOG.md` § Leads MEMBER-role enforcement addendum → Post-apply verification), plus a ninth, `import_leads_chunk`, which shipped after that pass and self-checks org membership (see `docs/ADDENDA_LOG.md` § CSV import chunk-write RPC → `get_advisors` after apply). Revisit the performance items if row counts reach the thousands. Full history: `docs/ADDENDA_LOG.md` § Known Gaps — Full Historical Record, § Prompt 15b addendum.

- **No `robots.txt` exists, deliberately.** ⬜ Decision recorded, not an oversight (2026-08-15) — `layout.tsx` sets `robots: { index: false, follow: false }`, which is the correct tool for a login-gated app. A `robots.txt` carrying `Disallow: /` was considered and rejected: it would stop crawlers fetching the page at all and therefore stop them ever reading the `noindex`, so a URL can still get indexed from an external link with no snippet. The two signals conflict rather than reinforce. Link unfurls are unaffected by either — Slack and Facebook read OG tags directly and do not apply robots rules. Revisit only if the app ever gets a genuinely public marketing route, which would need indexing rules per-path rather than one blanket meta tag. Full history: `docs/ADDENDA_LOG.md` § Brand application pass.
- **No static OG PNG exists.** ⬜ Deferred (2026-08-14) — `opengraph-image.tsx` covers every link shared from the app, which is the case that matters. A flat file is only needed if the mark goes into an email template or a deck. Generate one from the route output at that point rather than maintaining a second artifact now.

- **The v2 primitives are now verified by eye — with three exceptions.** ✅ Mostly closed 2026-08-14 (second pass). The earlier "never composited" finding was environmental and did **not** survive: with the Browser pane actually open, `document.visibilityState` reads `visible`, the page hydrates, real geometry is non-zero and screenshots work. Confirmed visually on `/design` in both panes: both themes force correctly against the ambient theme, all four Button variants × four states render flat (`box-shadow: none` in every one), primary/danger contrast measured at 6.54:1 and 5.93:1 light / 5.26:1 and 5.50:1 dark (all pass WCAG AA), the keyboard focus ring paints on `:focus-visible`, Input error border + message render in `--danger`, the Going Cold dashed border shows on both Card and table row, Modal opens at Level 2 and closes via X and backdrop click, and Popover opens at a visibly lighter Level 1. ⬜ Still unverified: (1) **NavItem hover**, still untested. (2) **The Button loading spinner turning** — correct by design here, since this machine reports `prefers-reduced-motion: reduce` and the project's own rule clamps it; the element was proven to rotate when driven directly via the Web Animations API. Full detail: `docs/ADDENDA_LOG.md` § Design System v2 "Structural Neutral" — foundation layer.
- **No browser-based test runner exists, so nothing can assert that a focus ring actually *paints*.** ⬜ Open (2026-08-17) — narrowed from the "nothing tests that a focus ring paints" gap closed the same day. `src/components/ui/dropdown-menu.test.tsx` now catches the regression that actually happened (a row carrying `outline-none`) at the class level, and was proven to fail before being trusted. What is still unreachable is the paint itself: jsdom's `getComputedStyle` does not apply stylesheet rules and `vitest.config.mts` sets `css: false`, so an outline read is empty for correct and broken components alike. A real assertion needs Playwright or vitest browser mode; neither is installed, and adding a second runner is a standalone decision, not a side effect of a test fix. Full history: `docs/ADDENDA_LOG.md` § 2026-08-17 — a regression test for the focus-ring floor.
- **`KanbanColumn`'s shell is not a `Card`, and that is deliberate.** ⬜ No action needed in any future prompt — ruled **not a defect** by the owner on 2026-08-15, recorded here so it is not "fixed" by a later sweep. `Card` renders a `<div>`, whereas the column is a deliberate `<section>` landmark, and its recessed `bg-canvas-soft` is the opposite of `Card`'s `bg-canvas-pure`. (This bullet previously also tracked `NeedsReviewQueue`, closed by Prompt 2b, and `HelpTrigger`, closed by Prompt 2c — both are in the Resolved Items Archive.)
- **Existing components have no tests.** ⬜ Open (2026-08-14, count refreshed 2026-09-12) — Vitest + RTL cover the v2 primitives, plus the invite-list filter and the two lead-lifecycle role gates: **416 tests, 58 suites** as of 2026-09-12, counted from a real `npm test` — the eighteen since the last mark came with Shell/IA **Stage 2**, wiring the picked read panel and the picked edit drawer into production: `src/components/leads/profile/LeadProfilePanel.test.tsx` (8, on the jump strip's five sections in the shipped order, the counted controls' exact accessible names, no invented count on Brief or Notes, `aria-current` rather than `aria-pressed`, the four protocol shortcuts on the header row, a starred lead marked without spending `--accent`, and the metadata strip's four real `leads` columns formatted with the tenant's own settings), `src/components/leads/panel-width.test.ts` (4, a SOURCE-level assertion that all three lead overlays read one width constant and that none of them names a `max-w-` of its own — a second cap would not error, would not fail any render test, and would simply make the slot resize between reading and editing), and 6 added to `EditLeadDrawer.test.tsx` (3 → 9: which two groups start open, that the four stated field counts total 18, that all 18 fields stay MOUNTED with every group closed, that the posted key set is identical closed and open, that a closed group opens on click, and that Save is not a descendant of the scrolling element). **Five of the nine in that file were proven to fail** by switching `FormSection` to `{open ? children : null}` — including both parity diffs, which is the point: an unmounted input contributes nothing to `FormData`, so `updateLead` would read null for every column behind a closed group and NULL it on save. Two traps this pass hit are not in the counts and are worth knowing: a `useFormResetRestore` living in a Radix overlay's own component silently attaches NO reset listener, because the content is not mounted when its effect runs; and a child that reports a count from its render body (rather than an effect) hangs vitest with no output at all. Previously **398 tests, 56 suites** on 2026-09-12 — the six before that came with the review follow-up to Shell/IA prompt 4: `src/app/(dev)/shell/form/FullFormVariant.test.tsx` (6, on the real-scale eighteen-field drawer at `/shell/form/full` — that all 18 controls stay MOUNTED with two groups collapsed, that exactly the two working groups start open, that each closed group states its own field count and the four counts total 18, that a closed group opens on click, that all three `<select>`s and the Radix `Checkbox` survive a real `form.reset()`, and that the submit button is not a descendant of the scrolling element). **One was proven to fail**, and it is the one that matters: switching a collapsed panel from `hidden` to `{open ? … : null}` dropped the mounted count from 18 to 12 — an unmounted input contributes nothing to `FormData`, so `updateLead` would read null for every column behind a closed group and NULL it on save. Collapsible sections are a brand-new route to the silent data-loss bug CLAUDE.md § Form/Action Field Parity exists to stop, and Stage 2's parity diff must be run with every group CLOSED. Previously **392 tests, 55 suites** on 2026-09-11 — and the forty before that last mark came in two parts, which matters. **Thirty-six** came with Shell/IA Stage 1 prompt 4's form and Settings comps, again DEV-ONLY code: `src/app/(dev)/shell/form/FormVariants.test.tsx` (14 — four per container over Modal/Drawer/Inline: the shared six-field set, edit-is-create-with-values-in-it, the React 19 `form.reset()` survival of the assignee `<select>` and the starred Radix `Checkbox`, and an assignee the org can no longer resolve staying selectable; plus two on the fixtures), `src/app/(dev)/shell/settings/SettingsVariants.test.tsx` (15 — three per variant on the shared section set, the absence of Team/API-keys/Account and the absence of any `input[type=file]`, then Rail's own pane swap and its `aria-current="page"`, and two on the org-profile selects), and two pure ones, `src/app/(dev)/shell/form/preview/resolve-mode.test.ts` (4) and `src/app/(dev)/shell/settings/preview/resolve-org.test.ts` (3), which pin that all three variant pages read `?long=1` through one function — if they disagreed, the width check would measure three different things while reporting one result. **Four of the thirty-six were proven to fail**, by removing `useFormResetRestore` from both shared blocks: exactly the three per-container reset tests plus the Settings one went red. Not covered here and deliberately not coverable here: whether any text collapses toward zero width. jsdom has no layout engine, so every `getBoundingClientRect()` is 0x0 and a width assertion in this suite would pass while measuring nothing — that is `npm run check:widths` (`scripts/check-comp-text-widths.mjs`), a real-browser check added by the same prompt and itself proven to fail before being trusted. **The other four were already in the tree and uncounted:** `src/app/demo/route.test.ts` (4) landed with `1face7a` without this figure being refreshed, so the true baseline before prompt 4 was **356 tests, 51 suites**, not the 352/50 recorded below — measured by re-running the suite with only prompt 4's four new test files removed. **Prompt 4 is not yet picked, so its tests stay with the comps as the record.** Previously **352 tests, 50 suites** on 2026-09-11 — the sixteen before that came with Shell/IA prompt 3's pipeline-card comps, again DEV-ONLY code: `src/app/(dev)/shell/pipeline/PipelineVariants.test.tsx` (16 — one that the fixture mixes every state the card must render, then five per variant: the fixed field set with no status badge, cold exactly on the overdue leads via `Card`'s own prop, no assignee row and no stray separator on a sparse lead, the 10-card cap expanding in place and collapsing back, and no control on a short column). One test failed first, in all three variants, on a fixture assumption rather than the code: a sparse lead chosen for the test ranked eleventh in its column and was hidden behind the cap. Not covered, and noted in the addendum: the Grouped meta line's width floors, which only browser width reads caught. **Variant Grouped was picked on 2026-09-11, so they stay with the comps as the record.** Previously **336 tests, 49 suites** on 2026-09-11 — the last two came with the Shell/IA prompt 2 close-out: 2 added to `src/app/(dev)/shell/detail/DetailVariants.test.tsx` for the section counts in Variant A's jump strip (exact accessible names, and a visible count on Tasks/Enquiries/Activity but none on Brief or Notes), both proven to fail before the counts existed; two existing jump tests were updated to the new names. Previously **334 tests, 49 suites** on 2026-09-09 — the eighteen before those came with Shell/IA Stage 1 prompt 2's detail-panel comps, and are unusual in covering DEV-ONLY code: `src/app/(dev)/shell/detail/DetailVariants.test.tsx` (13, one per variant's answer to section navigation) and `src/app/(dev)/shell/detail/preview/PanelChrome.test.tsx` (5, on the protocol hrefs and the real-column metadata strip). They exist because the browser could not verify them — the Browser pane was hidden all session, so Chromium fired no `requestAnimationFrame`, React never hydrated (measured `hydrated: false` on a live button) and every click no-opped. Two of the eighteen failed first and found real defects: a `$31,500.00` expectation against a `maximumFractionDigits: 0` formatter, and an accordion header whose accessible name computed as the single word `"Tasks3"` from two adjacent spans. **Variant A was picked on 2026-09-09, so they stay with the comps as the record of the comparison.** Previously **316 tests, 47 suites** on 2026-09-08 — the eighteen before those came with the Avatar primitive: `src/components/ui/Avatar.test.tsx` (18, covering initials derivation, the deterministic FNV-1a hue mapping over the six decorative pill hues, and the two whitespace regressions — the hash key trimmed but did not collapse inner whitespace, so `"Ada Lovelace"` and `"  ada   lovelace  "` rendered the same initials on two different hues, caught on `/design` and not by the suite as first written). Previously **298 tests, 46 suites** on 2026-09-05 — the nine before those came with the command palette's third source: 4 added to `src/components/command/CommandBar.tsx`'s suite (8 to 12, covering the third grouped section, its no-empty-header degrade, continuous arrow keys across BOTH seams, that all three sources are fetched once per open, and that a prospect result pushes `/prospects?highlight=<id>` while opening no profile sheet) and a new `src/components/prospects/ProspectsTable.test.tsx` (5, on the shared arrival highlight) — **4 of the 5 proven to fail** with `highlighted` unwired, and the filter-clearing one proven to fail on its own with only `onArrive` removed. Previously **289 tests, 45 suites** on 2026-09-05 — the eleven before those came with webhook ingestion failure visibility: `src/app/api/v1/triage/[organization_id]/route.test.ts` (7, covering the alert-once-on-a-DB-failure path, the no-alert-on-a-401 rule, and that neither the signing key, the signature header value nor the enquiry's PII reaches a log line) and `src/lib/webhooks/ingest-lead.test.ts` (4, on the coded errors each fatal write site now throws) — 8 of the 11 proven to fail against the pre-fix tree, the other three being negative guards that could not have. Previously **278 tests, 43 suites** on 2026-09-05 — the twenty-eight before those came with the three-gap wave: `src/lib/reports/periods.test.ts` (12, pure calendar-boundary logic, including a lead closed one second either side of a month boundary in `America/Chicago`, and proven to fail before the module existed at all), `src/lib/leads/export-csv.test.ts` (12, pinning that the export column set IS `LEAD_COLUMNS` rather than a hand-list, plus RFC 4180 quoting and CSV-formula neutralisation) and `src/lib/leads/report-queries.test.ts` (4, asserting at the query itself that closed leads filter on `closed_at` and open pipeline on `created_at`, with an exclusive end bound). Previously **250 tests, 40 suites** on 2026-09-05 — the twelve before those came with the invite-only signup gate: `src/lib/invites/signup-gate.test.ts` (6, on the pure verdict, including that every refusal returns identical wording) and `src/lib/auth/signup-gate-action.test.ts` (6, driving the `signUp` Server Action with the page bypassed and asserting `auth.signUp` is never reached in any refusal). Previously **238 tests, 38 suites** on 2026-09-04 — the three before those came with the demo read-only badge (`src/components/shell/DemoModeBadge.test.tsx`, proven to fail with the badge removed), on top of the three that came with the public demo itself: `src/lib/activity/audio-transcription.test.ts` (3, source-level assertions that the is_demo guard sits above `resolveOrgCredential`, proven to fail before the guard existed). Previously **231 tests, 36 suites** on 2026-08-28 — the fifteen new ones then came with global search: `src/components/command/CommandBar.test.tsx` (8, covering the two grouped sections, the no-empty-header degrade, continuous arrow-key indexing across both groups, and that each source is fetched once per open rather than per keystroke), plus 4 added to `TasksSection.test.tsx` for the command-palette arrival highlight (one of them — the Completed-tab switch — proven to fail against the pre-fix code) and 3 to `src/lib/tasks/queries.test.ts` for `searchTasksForOrg`. Previously **216 tests, 35 suites** on 2026-08-27 — the five new ones then were `src/lib/organizations/org-options.test.ts`, added with the timezone-label fix and pinning that no curated zone renders an ambiguous city name. Previously **211 tests, 34 suites** on 2026-08-26. Seven new suites later the same day, with the controlled-form fix: `CreateLeadModal.test.tsx` (3), `EditLeadModal.test.tsx` (3, asserting the whole five-file form at once), `IdentityFields.test.tsx` (5), `InviteMemberForm.test.tsx` (4), `OrgDetailsPanel.test.tsx` (5), `AccountPanel.test.tsx` (5), `ApiKeysPanel.test.tsx` (5), plus 2 added to `TaskRow.test.tsx` — the nine `form.reset()` survival tests among them each proven to fail against the pre-fix code. Earlier that day **179 tests, 27 suites**. Three additions with prospect promotion: `src/lib/prospects/promote-payload.test.ts` (15, including the three-direction Promote form/action field-parity diff), `src/lib/prospects/sort.test.ts` (11, pinning nulls-last-in-both-directions), and `PromoteProspectModal.test.tsx` (7, two of which are the React 19 form-reset regression). Earlier that day **146 tests, 24 suites**, one addition with the `prospects` table: `src/lib/prospects/phone.test.ts` (3, pinning the digits-only normalization that the SQL generated column and the import RPC both duplicate). Previously **143 tests, 23 suites** on 2026-08-19. Three additions on 2026-08-19, with task edit/dismiss: `src/lib/tasks/actions.test.ts` (7, including an exact-key-set assertion that `updateTask` moves no lifecycle column), `src/lib/tasks/queries.test.ts` (4, asserting the `dismissed` filter is applied at the database in both task queries), and `TaskRow.test.tsx` (6). Earlier the same week: 126/20 on 2026-08-18. Three additions that day: `AssignmentField.test.tsx` (6, with `leads.assigned_to`), `MemberRow.test.tsx` (6, with team management — one of which was proven to fail against the pre-fix code rather than only to pass against the fix), and `src/lib/webhooks/signature.test.ts` (14, with HMAC webhook signing — its pinned digest confirmed independently via `openssl dgst -sha256 -hmac`, not copied from the implementation's own output). Earlier the same day: 112/19. Earlier marks: 100/17 after `94de14b` added `ArchiveControls.test.tsx` and `OutcomeFields.test.tsx` at 4 tests each, 92/15 after the four-gap wave, 82/13 at the shell redesign, 74/12 at the pre-auth v2 rollout, 67/10 at the stuck-invite fix, 62/9 at the primitive audit, 49/8 at Prompt 1. Everything predating them is untested. **This count is refreshed on every handoff audit** — the 92/15 mark survived two sessions after `94de14b` made it wrong, because the audit checked gaps the session had *touched* and never cross-checked a figure it had just measured.

- **Prospect promotion is not one cross-table transaction, so a lost race can orphan a lead.** ⬜ Deferred (2026-08-26) — `promoteProspect` inserts the `leads` row and its `lead_submissions` row, then claims the prospect with a guarded single-statement `update … where promoted_lead_id is null`. The claim itself is atomic and promoting twice is impossible, but if a concurrent promotion wins the race in between, the lead just created is real and unreferenced. The action handles that case explicitly — it logs the orphan id and tells the operator a duplicate lead exists, with a link to the original — rather than succeeding silently. A true fix is a `SECURITY DEFINER` RPC wrapping all three writes, which was out of scope for a migration fenced to two additive columns. The window is one round trip wide and needs two people promoting the same prospect within it. Full history: `docs/ADDENDA_LOG.md` § 2026-08-26 — Prospect promotion: the write path into `leads`.

- **The `prospects` duplicate-phone lookup has no index on the `leads` side.** ⬜ Deferred (2026-08-26) — `import_prospects_chunk` compares a normalized-digits expression against `leads.phone`, which would ideally be backed by a functional index on `leads`. Adding one is DDL on `leads` and was explicitly out of scope for that unit. Harmless at current scale: one lookup per imported CSV row, low hundreds of leads per tenant, and import is a manual operator action — the whole 122-row run was sub-second. Revisit only if a tenant's `leads` count reaches five figures. Full history: `docs/ADDENDA_LOG.md` § 2026-08-26 — `prospects`: cold-outreach staging, its RLS and its CSV import.


- **Any route under `(app)` without its own `loading.tsx` falls back to the Today agenda's skeleton.** ⬜ Registered 2026-08-28 — `src/app/(app)/loading.tsx` is deliberately shaped like `TodayAgenda`'s three-column layout, and Next uses the nearest ancestor boundary, so `/import`, `/prospects/import` and any future segment without its own file show a three-column agenda skeleton that matches nothing on the page. Found while A/B-testing the loading-skeleton flash: deleting the four route-level `loading.tsx` files did not remove the skeleton, it just swapped in the agenda shape (median 381ms vs 345ms), which is what exposed the fallback. The four files were restored. The fix is either a neutral shared `(app)/loading.tsx` with the Today shape moved into its own segment, or a `loading.tsx` per remaining route — a shape decision, not a bug fix, and it overlaps the Shell/IA pass below. Full history: `docs/ADDENDA_LOG.md` § 2026-08-28 — The loading-skeleton flash: a fixed auth tax on every navigation.
- **The `/prospects` table has no column resize and no column reorder.** ⬜ Registered 2026-08-27. **Sorting is not missing** — `ProspectsTable` ships sortable headers on four columns via `src/lib/prospects/sort.ts` (`name`, `city`, `status`, `rating`, nulls last in both directions); `review_count` and `created_at` are sortable in principle but deliberately have no header. What is absent is resize and reorder, and `src/components/ui/TableRow.tsx` is a bare structural shell that says so in its own comment — no sorting, selection or resizing, by design, built with the feature rather than ahead of it. So this is new work in the primitive plus the one caller, and it is the only table view in the app.
- **Shell/IA needs a dedicated redesign pass, not incremental patches.** ⬜ Registered 2026-08-27 — five separate complaints landed in one session: sidebar collapse-icon placement, an oversized New Lead button, no logo-click-to-collapse, avatar/Settings split between header and sidebar, and the Settings page's own layout. Treated as one discovery pass rather than five patches, because the last shell prompt's six binding decisions live in `docs/DESIGN.md` § The Application Shell and any of these changes edits them. Overlaps the org-switcher bullet above, whose reserved `WorkspaceBlock` slot is in the same region. **Scope widened 2026-08-28 by the owner:** beyond those five, the nav and sidebar are judged visually *plain* rather than broken — "looks fine, but a bit plain/simple" — so this pass is an aesthetic and information-architecture brief, not a defect list, and it explicitly includes the **mobile** navigation (the four-slot tab bar, whose slots were last declined reopening with `/reports` on 2026-08-19) alongside the desktop rail. Constraints that do not move: the collapse must keep animating `translate` on an overlaid rail and never a layout property; the state stays the `tg_sidebar` cookie read in the server layout, never `localStorage`; `<main>` stays `relative`; `--accent` stays limited to primary CTAs, active nav links, focus rings and inline links, so "less plain" cannot be bought with decorative colour. Full history: `docs/ADDENDA_LOG.md` § Application shell redesign — sidebar, header, mobile navigation, § Shell redesign close-out: cookie audit, collapse animation, focus ring, § 2026-08-17 — sidebar collapse: a transform-based overlaid rail. **Scope fixed 2026-09-06, before any design work starts:** the **org switcher is explicitly out of scope for this pass** — no persisted-active-org migration, and `WorkspaceBlock` stays the static placeholder it is today; the org-switcher bullet above is untouched by this pass and keeps its own separate life. **`/demo` needs no shell-specific treatment** — it renders the same sidebar and header as the main app, and the only demo-specific element stays the `Read-only` badge already shipped. The pass runs as **two stages**: **Stage 1 — design exploration**, which is dev-only routes over mock or read-only data producing **2–3 named variants**, with **no changes to `src/lib/actions/`, no RLS, no migration, and no production route touched**, covering the shell (desktop rail + header + the four-slot mobile tab bar), the lead/contact detail panel, pipeline card density, and the lead/contact form together with the Settings page layout; then **Stage 2 — production wiring**, a separate prompt pack that takes the one picked direction into the real app. Stage 1 shipping is what earns this pass its own `docs/addenda/` entry; until then this bullet is the record. **Progress 2026-09-12: all four Stage 1 prompts are built. The form container is now picked; the Settings layout is the one thing in Stage 1 still open.** Picks so far: the shell's **Variant C — Quiet** (2026-09-07), the detail panel's **Variant A — Jump strip** (2026-09-09), and the pipeline card's **Variant Grouped** (2026-09-11) — a header row of name, star and revenue over one meta line of company · date · assignee, 65px per card and 8 cards visible per column at 1440×800, with a `+N more` control after 10 cards. Its known cost is a truncated company name on assigned leads, accepted because the board will hold only a handful of leads for weeks to months. Carried into Stage 2 from prompt 3: `KanbanCard` and `FocusListCard` hold two identical copies of the field block, and `FocusListCard`'s status `<select>` has no place yet in Grouped's two rows. **Prompt 4 shipped 2026-09-11 and is the one Stage 1 prompt with NO pick yet** — its review has not happened. It built two comp sets over static fixtures: the lead/contact form at `/shell/form` as three CONTAINERS for one form (Modal, Drawer, Inline — the fields, their order and their wiring come from one shared `preview/FormBody.tsx`, and Edit is Create with values in it rather than a second design), and Settings → Org Profile & Branding at `/shell/settings` as three LAYOUTS for the same three sections (Stacked, Rail, Split). Measured at 1440×800: the form containers are 512 / 447 / 672px wide and all three fit six fields and the submit row; Settings puts its first control 127 / 127 / 43px down, and Stacked is the only one whose content (893px) overflows the 668px box. Deliberately absent rather than stubbed: Team, API keys and Account, and any per-organization logo upload — `public.organizations` has no logo column, no bucket and no upload action, so drawing one would be inventing a data model. Also new, and useful past this prompt: **`npm run check:widths`**, a real-browser layout check that fails when any element owning three or more characters of its own text renders under 24px wide, at 1440×800 and 1024×800, over deliberately longest-value fixtures. It exists because prompt 3 needed three measured drafts before a field stopped collapsing (one rejected draft measured a company name at 1px) and no test in this repo could ever catch that class. Carried into Stage 2 from prompt 4: the comp field set was six fields while the real forms post eight and eighteen. ✅ **Closed 2026-09-12 by Stage 2.** The wired forms carry the real field sets, and the parity diff was run against them in both directions with every collapsible group CLOSED: the edit form's 18 rendered `name=`s equal `updateLead`'s 18 `formData.get()`s exactly, and the create form's 8 equal `createLead`'s 8 exactly — all four difference lists empty. The create drawer deliberately stays at eight: `createLead` accepts eight and nothing else, so an input for a column it does not read would be the same parity failure in the opposite direction. ⬜ Still open from prompt 4: the Settings comps cover one of the shipped page's four panels, so where the other three sit inside the picked layout is still open — and the Settings layout itself is still unpicked. **Prompt 4's form container picked 2026-09-12: Variant Drawer — "everything in the drawer".** Reading a lead and editing one share a single address on screen, which is the slot the picked detail panel (Variant A — Jump strip) already owns. The alternative considered and **declined** was a wide full-page read view with editing still in a drawer: it would reopen the 2026-09-09 detail-panel pick and split one lead across two places. Modal and Inline are kept as the record, not maintained. **The Settings layout is still unpicked**, though the reviewer's leaning is **Split**, which already IS the responsive answer they described — two columns at `md` and up, stacking below it, one variant covering both, so no second variant is needed for small screens. Two measured findings came out of the same review. Finding one: prompt 4's Drawer comp was `max-w-md`, **447px**, which is the same width as the shipped `Modal` primitive (448px) the reviewer called too narrow — the comp reproduced the complaint. It now ramps `w-full` → `sm:max-w-lg` → `lg:max-w-2xl` (672px at 1440, 390px full-width on a phone), **which forced a Stage 2 decision. ✅ Closed 2026-09-12 by Stage 2: `ProfileSheet` was widened to that same ramp rather than the drawer being capped down.** The ramp is now one exported constant, `LEAD_PANEL_WIDTH` in `src/components/leads/panel-width.ts`, read by the read panel, the edit drawer and the create drawer alike, with a source-level test asserting none of the three names a width of its own. Measured in a real headless Chrome at 1440×800: **672px each, identical**. Both are now the same primitive too — `src/components/ui/sheet.tsx` gained a `side` prop, which its own comment had reserved for exactly this caller — so the read panel gained a focus trap it never had, and the hand-rolled `createPortal` it used to need is gone with the native `<dialog>` that caused it. Finding two, from a new route `/shell/form/full` carrying the **real eighteen-field** edit form in the picked drawer with a switchable width: going from 448px to 768px changes the height by **zero pixels**, because the two-column grid runs off Tailwind's `sm:` VIEWPORT breakpoint rather than the panel — a container query would couple them and is Stage 2 work. What does buy height is the two-up grid (18 rows become 10) and collapsing the two rarely-touched groups (425px of overflow becomes 92px); what actually answers "I always have to scroll to the bottom" is neither, it is the **pinned action bar**, Save being outside the scrolling element. Collapsible groups also introduce a new way to hit the Form/Action field-parity data-loss bug, so a closed group keeps its inputs MOUNTED and merely `hidden`; Stage 2's parity diff must run with every group closed. **STAGE 2 HAS STARTED, 2026-09-12, with the lead surface.** The picked read panel (Variant A, the jump strip) and the picked form container (Variant Drawer) are wired into production together, because they share one slot and could not be wired apart. `ProfileSheet` is the jump-strip panel over real lead data with real section counts; `EditLeadDrawer` and `CreateLeadDrawer` replace `EditLeadModal` and `CreateLeadModal`; the directory `components/leads/edit-modal/` is now `edit-form/` and its five field-group files kept their field sets exactly. **No schema, no migration, no RLS, no grant, and `src/lib/leads/actions.ts` byte-for-byte unchanged.** ⬜ Still open in Stage 2, in their own prompts: the shell (Variant C — Quiet), the pipeline card (Variant Grouped, whose two shipped cards still hold identical copies of the field block and whose status `<select>` on `FocusListCard` has no place yet in Grouped's two rows), and Settings, which is not even picked. ⬜ Also carried: the read panel still has no way INTO the edit drawer — the handoff runs edit → read only, exactly as it always did, and the `?leadId=` deep link lands on a panel with no edit control. Unchanged behaviour rather than a regression, and it belongs with the shell wiring prompt that owns the surrounding navigation. Full history: `docs/ADDENDA_LOG.md` § 2026-09-11 — Shell/IA Stage 1, prompt 3 of 4: the pipeline card, and why Grouped won, § 2026-09-11 — Shell/IA Stage 1, prompt 4 of 4: the form's container, the Settings layout, and a width check a unit test cannot be, § 2026-09-12 — The lead form at real scale: what actually causes the scrolling, and why width was not it, § 2026-09-12 — Shell/IA Stage 2: the lead read panel and edit drawer, wired at one width.

- **The public demo cannot write anything, so no create/edit flow is visible in it.** ⬜ Deliberate v1 non-goal (2026-09-04) — `/demo`'s identity holds the `demo_readonly` Postgres role, which has `SELECT` and nothing else, so a visitor sees the New Lead button and the task Dismiss control but every attempt is refused by Postgres with `42501`. The alternative was carving `activity_logs` and storage out of the guarantee, which would have handed a stranger an unbounded way to fill both with no reset job to clean it. If a writable demo is ever wanted, the answer is a per-visitor ephemeral org, not a hole in this role. Full history: `docs/addenda/2026-09.md` § 2026-09-04 — The public read-only demo identity.
- **The demo shows an error page, not an explanation, when a write is refused.** ✅ Partly closed / ⬜ still open (2026-09-04) — a refused write surfaces as the app's generic error boundary, because nothing in the UI knows the current identity is read-only. A visitor cannot tell "this demo is read-only" from "this app is broken", which is the wrong impression for a portfolio piece. ✅ The warning half shipped the same day: a `Read-only` badge sits in the shell header for the demo tenant only (`src/components/shell/DemoModeBadge.tsx`). It says `Read-only` and deliberately not `Demo` — the sidebar already carries the org name, and read-only is the one fact a visitor cannot get anywhere else; the short text also fits a 375px header with no abbreviation. Neutral pill with a hairline, NOT the orange one: orange shipped first and collided with the SLA `Overdue` badges a few pixels below it on the Today view. It is in the header rather than the sidebar because `WorkspaceBlock` is `sr-only` when collapsed and absent on mobile. ⬜ Still open: the refusal ITSELF is unchanged — a write that Postgres denies still surfaces as the app generic error boundary, so a visitor who clicks anyway gets a crash-looking page rather than "the demo is read-only". A friendlier refusal needs the error boundary to know the session mode, which is a wider change than a badge. Presentation only either way; the database boundary is unaffected.
- **`robots: noindex` still covers `/demo`, and that was a decision.** ⬜ Decision recorded, not an oversight (2026-09-04) — this file's "No `robots.txt` exists, deliberately" entry named "a genuinely public marketing route" as its one revisit trigger, and `/demo` is now that route. Revisited and deliberately unchanged: `/demo` returns a 302 with no HTML body, so there is nothing there to index anyway, and the tekguyz.com case-study page is what should rank. Per-path indexing rules become worth writing when the real marketing landing page in `docs/ROADMAP.md` P1 ships, not before.
- **Custom Postgres roles live in a migration, not `supabase/roles.sql`.** ⬜ Open (2026-09-04) — the Supabase CLI's convention is a `supabase/roles.sql` file pushed with `--include-roles`; this project has no such file, and `create role demo_readonly` lives in migration `20260904120000` instead. That works on hosted Supabase and is applied there, but `supabase db reset` against a local database would not recreate the role. Harmless today because this project has no local-database workflow. Restructure only if one is ever adopted.
- **Remote migration history carries `20260707215206`, which is absent from this repo.** ⬜ Open (2026-09-04) — found while trying to run `supabase db push --dry-run`, which refuses to proceed because of it. Deliberately not repaired: `supabase migration repair` writes to the remote migration history table, which is a human decision, not an agent action. Consequence today is that the CLI's push path is unusable and migrations are applied by hand through the SQL Editor. `scripts/dryrun-migration.ts` covers the verification the CLI would otherwise have provided.
- **`scripts/dryrun-migration.ts` cannot dry-run roles or grants.** ⬜ Open by nature (2026-09-04) — `create role` and `grant` are cluster-level, not session-local, so a `pg_temp` replica cannot host them; only table DDL is genuinely dry-run. Stated in the script rather than hidden. There is no fix short of a throwaway database, which is not worth it for this project's migration volume.
